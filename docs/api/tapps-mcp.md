# `packages.tapps-mcp.src.tapps_mcp`

TappsMCP: MCP server providing code quality tools.

> **Note:** This module re-exports symbols from other modules. See source modules for full documentation.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive`

Backward-compatible re-export — delegates to tapps_core.adaptive.

> **Note:** This module re-exports symbols from other modules. See source modules for full documentation.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive.models`

Backward-compatible re-export.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive.persistence`

Backward-compatible re-export.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive.protocols`

Backward-compatible re-export.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive.scoring_engine`

Backward-compatible re-export.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive.scoring_wrapper`

Backward-compatible re-export.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive.voting_engine`

Backward-compatible re-export.

Also wires ``_build_default_matrix`` so that patches against this
module propagate to the core implementation.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.adaptive.weight_distributor`

Backward-compatible re-export.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark`

Benchmark infrastructure for evaluating TappsMCP context file effectiveness.

> **Note:** This module re-exports symbols from other modules. See source modules for full documentation.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.ablation`

Section ablation testing for template optimization.

Systematically removes one section at a time from a template and
evaluates the impact on benchmark resolution rates. Identifies
sections that are essential, neutral, or harmful to performance.

## Classes

### `AblationConfig`(BaseModel)

Configuration for an ablation test run.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `base_template` | `str` | `Field(description='Full template content to ablate.')` |
| `sections` | `list[str]` | `Field(description='Section names to test removing.')` |
| `benchmark_config` | `BenchmarkConfig` | `Field(description='Benchmark configuration.')` |
| `baseline_results` | `list[BenchmarkResult] | None` | `Field(default=None, description='Pre-computed baseline results (full template). Computed automatically if not provided.')` |

### `AblationResult`(BaseModel)

Result of removing a single section from the template.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `removed_section` | `str` | `Field(description='Name of the removed section.')` |
| `resolution_rate` | `float` | `Field(description='Resolution rate with section removed.')` |
| `delta_vs_full` | `float` | `Field(description='Delta compared to full template (negative = worse without section).')` |
| `delta_vs_none` | `float` | `Field(description='Delta compared to no-context baseline.')` |
| `cost_delta` | `float` | `Field(default=0.0, description='Cost delta vs full template.')` |
| `recommendation` | `str` | `Field(description="Classification: 'essential', 'neutral', or 'harmful'.")` |

### `AblationRunner`

Runs section ablation tests on a template.

**Methods:**

#### async `run_ablation`

```python
async def run_ablation(self, config: AblationConfig, evaluator: _EvaluatorProtocol) -> list[AblationResult]:
```

Run ablation tests, removing one section at a time.

If ``config.baseline_results`` is not provided, the full template
is evaluated first as the baseline.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `config` | `AblationConfig` | Ablation configuration. | *required* |
| `evaluator` | `_EvaluatorProtocol` | Evaluator (mock or real) for running benchmarks. | *required* |

**Returns:** `list[AblationResult]` - List of ablation results, one per section.

#### `generate_optimal_template`

```python
def generate_optimal_template(self, results: list[AblationResult], base_template: str) -> str:
```

Generate an optimal template by removing harmful sections.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[AblationResult]` | Ablation results from ``run_ablation``. | *required* |
| `base_template` | `str` | Original full template. | *required* |

**Returns:** `str` - Template with harmful sections removed.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.adaptive_feedback`

Adaptive weight feedback loop from benchmark measurements.

Translates tool effectiveness measurements into scoring category weight
adjustments, closing the loop between benchmarking and the adaptive
scoring engine.

## Classes

### `BenchmarkFeedback`(BaseModel)

Feedback from benchmark measurements for the adaptive engine.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `tool_name` | `str` | `Field(description='Tool this feedback applies to.')` |
| `impact_score` | `float` | `Field(description='Measured impact score.')` |
| `cost_ratio` | `float` | `Field(ge=0.0, le=1.0, description='Cost ratio (token overhead relative to total).')` |
| `category_impacts` | `dict[str, float]` | `Field(description='Impact scores broken down by task category.')` |
| `source` | `str` | `Field(default='benchmark', description='Source of this feedback.')` |
| `sample_size` | `int` | `Field(ge=0, description='Number of tasks measured.')` |
| `confidence` | `float` | `Field(ge=0.0, le=1.0, description='Confidence in the measurement.')` |

### `AdaptiveFeedbackGenerator`

Generate adaptive scoring feedback from benchmark results.

**Methods:**

#### `generate_feedback`

```python
def generate_feedback(self, tool_report: ToolEffectivenessReport) -> list[BenchmarkFeedback]:
```

Generate per-tool feedback from effectiveness measurements.

Each tool gets a BenchmarkFeedback entry with its measured impact
broken down by scoring category, plus a confidence score based
on sample size.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tool_report` | `ToolEffectivenessReport` | Tool effectiveness report from benchmark. | *required* |

**Returns:** `list[BenchmarkFeedback]` - List of feedback entries, one per tool.

#### `generate_weight_adjustments`

```python
def generate_weight_adjustments(self, feedback: list[BenchmarkFeedback]) -> dict[str, float]:
```

Generate scoring category weight adjustments from feedback.

Aggregates per-tool category impacts into overall weight deltas.
Adjustments are capped at ``_MAX_ADJUSTMENT`` per category and
normalized to sum to zero (so overall weight sum is preserved).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `feedback` | `list[BenchmarkFeedback]` | Benchmark feedback entries. | *required* |

**Returns:** `dict[str, float]` - Mapping of category name to weight adjustment delta.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.analyzer`

Results aggregation and comparison for benchmark runs.

Provides `[`ResultsAnalyzer`](#resultsanalyzer)` which aggregates raw
``BenchmarkResult`` lists into summaries, computes condition deltas,
runs McNemar's test for statistical significance, and compares
engagement levels.

## Classes

### `ResultsAnalyzer`

Aggregate, compare, and analyze benchmark results.

**Methods:**

#### `aggregate`

```python
def aggregate(self, results: list[BenchmarkResult]) -> BenchmarkSummary:
```

Aggregate a list of results into a summary.

Computes totals, averages, and per-repo breakdowns.  The
``context_mode`` and ``engagement_level`` are taken from the
first result (all results in a run should share these).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[BenchmarkResult]` | Individual benchmark results to aggregate. | *required* |

**Returns:** `BenchmarkSummary` - Aggregated ``BenchmarkSummary``.

#### `compare_conditions`

```python
def compare_conditions(self, baseline: list[BenchmarkResult], treatment: list[BenchmarkResult]) -> ComparisonReport:
```

Compare baseline vs treatment benchmark runs.

Computes resolution, token, and cost deltas.  Runs McNemar's
test on paired binary outcomes for statistical significance.

Both lists must cover the same set of instance_ids for the
McNemar test to be meaningful.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `baseline` | `list[BenchmarkResult]` | Results from the baseline (control) run. | *required* |
| `treatment` | `list[BenchmarkResult]` | Results from the treatment (experimental) run. | *required* |

**Returns:** `ComparisonReport` - ``ComparisonReport`` with deltas and significance info.

#### `compare_engagement_levels`

```python
def compare_engagement_levels(self, results_by_level: dict[str, list[BenchmarkResult]]) -> EngagementReport:
```

Compare results across engagement levels.

Aggregates each level's results, then recommends the level
with the best resolution_rate / avg_cost ratio (efficiency).
When cost is zero, resolution rate alone determines the
winner.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results_by_level` | `dict[str, list[BenchmarkResult]]` | Mapping of engagement level name to results for that level. | *required* |

**Returns:** `EngagementReport` - ``EngagementReport`` with per-level summaries and a
    recommendation.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.call_patterns`

Call pattern analysis for MCP tool benchmarks.

Analyzes how tools are called relative to expectations, detecting
overcalls, undercalls, common call sequences, and computing efficiency
metrics. Generates actionable recommendations for tool usage.

## Classes

### `CallPattern`(BaseModel)

Call pattern analysis for a single task.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `task_id` | `str` | `Field(description='Task identifier.')` |
| `tools_called` | `list[str]` | `Field(description='Tools that were actually called.')` |
| `tools_expected` | `list[str]` | `Field(description='Tools that were expected to be called.')` |
| `overcalls` | `list[str]` | `Field(description='Tools called but not expected.')` |
| `undercalls` | `list[str]` | `Field(description='Tools expected but not called.')` |
| `call_efficiency` | `float` | `Field(ge=0.0, le=1.0, description='Ratio of useful calls to total calls (0.0-1.0).')` |

### `CallPatternReport`(BaseModel)

Aggregated call pattern analysis across all tasks.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `patterns` | `list[CallPattern]` | `Field(description='Per-task call patterns.')` |
| `avg_efficiency` | `float` | `Field(ge=0.0, le=1.0, description='Average call efficiency across all tasks.')` |
| `most_overcalled` | `list[tuple[str, int]]` | `Field(description='Tools most frequently called unnecessarily, as (name, count) pairs.')` |
| `most_undercalled` | `list[tuple[str, int]]` | `Field(description='Tools most frequently missing when expected, as (name, count) pairs.')` |
| `common_sequences` | `list[list[str]]` | `Field(description='Most common tool call sequences observed.')` |

### `CallRecommendation`(BaseModel)

Recommendation for adjusting tool usage.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `tool_name` | `str` | `Field(description='Tool this recommendation applies to.')` |
| `recommendation` | `str` | `Field(description="Action: 'increase', 'decrease', or 'keep'.")` |
| `rationale` | `str` | `Field(description='Explanation for the recommendation.')` |

### `CallPatternAnalyzer`

Analyze tool call patterns relative to expectations.

**Methods:**

#### `analyze`

```python
def analyze(self, results: list[ToolImpactResult], tasks: list[ToolTask]) -> CallPatternReport:
```

Analyze call patterns across results and tasks.

Compares actual tool calls against expected tools for each task,
computing per-task efficiency and aggregate overcall/undercall
statistics.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[ToolImpactResult]` | Tool impact results from evaluation. | *required* |
| `tasks` | `list[ToolTask]` | Task definitions with expected tool lists. | *required* |

**Returns:** `CallPatternReport` - Aggregated call pattern report.

#### `generate_recommendations`

```python
def generate_recommendations(self, report: CallPatternReport) -> list[CallRecommendation]:
```

Generate tool usage recommendations from a call pattern report.

Tools that are frequently undercalled get an "increase" recommendation.
Tools that are frequently overcalled get a "decrease" recommendation.
All other tools get a "keep" recommendation.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `report` | `CallPatternReport` | Call pattern report to analyze. | *required* |

**Returns:** `list[CallRecommendation]` - List of recommendations, one per tool mentioned in patterns.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.checklist_calibrator`

Data-driven checklist tier calibration.

Uses benchmark measurements (tool impact, call frequency, cost) to
recommend whether each tool should be 'required', 'recommended', or
'optional' in TappsMCP's task-completion checklist. Thresholds adjust
by engagement level.

## Classes

### `ToolTierClassification`(BaseModel)

Classification of a tool into a checklist tier.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `tool_name` | `str` | `Field(description='Name of the tool.')` |
| `measured_impact` | `float` | `Field(description='Measured impact score from benchmark.')` |
| `measured_cost` | `float` | `Field(description='TAP-1800: signed average token cost (tokens, not a ratio). Negative values indicate token-saving tools. Stored as float for back-compat with prior schema; values are integers in practice.')` |
| `call_frequency` | `float` | `Field(ge=0.0, le=1.0, description='Fraction of tasks where the tool was called.')` |
| `recommended_tier` | `str` | `Field(description="Recommended tier: 'required', 'recommended', or 'optional'.")` |
| `current_tier` | `str` | `Field(description='Current tier in the checklist.')` |
| `tier_change` | `str | None` | `Field(default=None, description="Change direction: 'promoted', 'demoted', or 'unchanged'.")` |
| `justification` | `str` | `Field(description='Explanation for the tier recommendation.')` |

### `ChecklistCalibration`(BaseModel)

Complete calibration result for all tools.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `classifications` | `list[ToolTierClassification]` | `Field(description='Per-tool tier classifications.')` |
| `engagement_level` | `str` | `Field(description='Engagement level used for threshold calculation.')` |

### `ChecklistCalibrator`

Calibrate checklist tool tiers based on benchmark measurements.

**Methods:**

#### `calibrate_tiers`

```python
def calibrate_tiers(self, tool_rankings: list[ToolRanking], call_report: CallPatternReport, engagement_level: str = 'medium') -> ChecklistCalibration:
```

Classify tools into checklist tiers using measured data.

Tier rules (before engagement-level adjustment):
- Impact > threshold AND avg token cost < ``_COST_THRESHOLD_TOKENS``: "required"
- Impact > 0% OR frequently called: "recommended"
- Impact <= 0% or rarely called: "optional"

Note (TAP-1800): the previous "cost less than 15%" rule used a
``x / (x + 1000)`` pseudo-ratio that crossed 0.15 at ~176 tokens, so
any moderately chatty tool was disqualified from "required" regardless
of impact. Cost is now an absolute token budget; token-saving tools
(signed negative cost from TAP-1799) always pass the cost check.

Impact threshold varies by engagement level:
- high: 1%
- medium: 3%
- low: 5%

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tool_rankings` | `list[ToolRanking]` | Per-tool impact rankings from benchmark. | *required* |
| `call_report` | `CallPatternReport` | Call pattern analysis for frequency data. | *required* |
| `engagement_level` | `str` | Engagement level for threshold tuning. | 'medium' |

**Returns:** `ChecklistCalibration` - Complete calibration with per-tool classifications.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.cli_commands`

CLI commands for the benchmark subsystem.

## Functions

### `benchmark_group`

```python
def benchmark_group() -> None:
```

Benchmark infrastructure for evaluating TappsMCP context files.

**Returns:** `None`

### `run_benchmark`

```python
def run_benchmark(dataset: str, context_mode: str, engagement_level: str, subset: int, workers: int, output_dir: str, run_id: str, mock: bool) -> None:
```

Run benchmark evaluation on AGENTBench instances.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `dataset` | `str` |  | *required* |
| `context_mode` | `str` |  | *required* |
| `engagement_level` | `str` |  | *required* |
| `subset` | `int` |  | *required* |
| `workers` | `int` |  | *required* |
| `output_dir` | `str` |  | *required* |
| `run_id` | `str` |  | *required* |
| `mock` | `bool` |  | *required* |

**Returns:** `None`

### `analyze_results`

```python
def analyze_results(run_id: str, compare: str, output_format: str, output_dir: str) -> None:
```

Analyze benchmark results and generate reports.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `run_id` | `str` |  | *required* |
| `compare` | `str` |  | *required* |
| `output_format` | `str` |  | *required* |
| `output_dir` | `str` |  | *required* |

**Returns:** `None`

### `generate_report`

```python
def generate_report(output_file: str, output_dir: str, include_redundancy: bool) -> None:
```

Generate a summary report of all benchmark runs.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `output_file` | `str` |  | *required* |
| `output_dir` | `str` |  | *required* |
| `include_redundancy` | `bool` |  | *required* |

**Returns:** `None`

### `tools_group`

```python
def tools_group() -> None:
```

Tool effectiveness benchmarking commands.

**Returns:** `None`

### `tools_report`

```python
def tools_report(output_format: str, tool_list: str) -> None:
```

Generate a full tool effectiveness report.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `output_format` | `str` |  | *required* |
| `tool_list` | `str` |  | *required* |

**Returns:** `None`

### `tools_rank`

```python
def tools_rank() -> None:
```

Show tool effectiveness ranking.

**Returns:** `None`

### `tools_calibrate`

```python
def tools_calibrate(engagement_level: str) -> None:
```

Generate data-driven checklist tier calibration.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` |  | *required* |

**Returns:** `None`

### `template_group`

```python
def template_group() -> None:
```

Template optimization and version management commands.

**Returns:** `None`

### `template_optimize`

```python
def template_optimize(engagement_level: str, repo_path: str, db_path: str, mock: bool) -> None:
```

Run the template optimization pipeline.

Analyzes the current template for redundancy, records a new version,
evaluates via benchmarks, and recommends improvements.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` |  | *required* |
| `repo_path` | `str` |  | *required* |
| `db_path` | `str` |  | *required* |
| `mock` | `bool` |  | *required* |

**Returns:** `None`

### `template_ablate`

```python
def template_ablate(engagement_level: str, subset: int) -> None:
```

Run section ablation analysis on a template.

Removes each section one at a time and measures the impact
on benchmark resolution rate.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` |  | *required* |
| `subset` | `int` |  | *required* |

**Returns:** `None`

### `template_compare`

```python
def template_compare(version_a: int, version_b: int, db_path: str) -> None:
```

Compare two template versions.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `version_a` | `int` |  | *required* |
| `version_b` | `int` |  | *required* |
| `db_path` | `str` |  | *required* |

**Returns:** `None`

### `template_history`

```python
def template_history(engagement_level: str, limit: int, db_path: str) -> None:
```

Show template version history.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` |  | *required* |
| `limit` | `int` |  | *required* |
| `db_path` | `str` |  | *required* |

**Returns:** `None`

---

*Documentation coverage: 100.0%*
*Quality: 13 functions missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.config`

Benchmark configuration loading and defaults.

## Functions

### `load_benchmark_config`

```python
def load_benchmark_config(project_root: Path | None = None, overrides: dict[str, Any] | None = None) -> BenchmarkConfig:
```

Load benchmark configuration with correct precedence.

Precedence (highest to lowest):
1. Explicit overrides
2. ``.tapps-mcp.yaml`` ``benchmark:`` section
3. Built-in defaults

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `Path \| None` | Project root directory.  Falls back to CWD. | None |
| `overrides` | `dict[str, Any] \| None` | Field-level overrides applied on top of YAML values. | None |

**Returns:** `BenchmarkConfig` - Fully resolved ``BenchmarkConfig``.

## Constants

| Name | Type | Value |
|------|------|-------|
| `DEFAULT_CONFIG` | `` | `BenchmarkConfig()` |

---

*Documentation coverage: 100.0%*
*Quality: 1 function missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.context_injector`

Context injection engine for the benchmark subsystem.

Generates TappsMCP AGENTS.md files and injects them into benchmark
repository checkouts. Also provides redundancy analysis to measure
overlap between injected context and existing repository documentation.

## Classes

### `SectionRedundancy`(BaseModel)

Per-section redundancy score for an AGENTS.md section.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `section_name` | `str` | `Field(description='Name of the AGENTS.md section.')` |
| `redundancy_score` | `float` | `Field(ge=0.0, le=1.0, description='Redundancy score (0.0-1.0).')` |
| `recommendation` | `str` | `Field(description="Action recommendation: 'keep', 'reduce', or 'remove'.")` |

### `ContextInjector`

Generate and inject TappsMCP context files into repositories.

**Methods:**

#### `__init__`

```python
def __init__(self, engagement_level: str = 'medium') -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` |  | 'medium' |

**Returns:** `None`

#### `generate_tapps_context`

```python
def generate_tapps_context(self, repo_path: Path) -> str:
```

Generate TappsMCP AGENTS.md content for a repository.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `repo_path` | `Path` | Path to the repository checkout (unused today but reserved for future project-type-aware generation). | *required* |

**Returns:** `str` - The AGENTS.md template content for the configured engagement
    level.

#### `inject_context`

```python
def inject_context(self, repo_path: Path, content: str, filename: str = 'AGENTS.md') -> Path:
```

Write context content into a repository checkout.

If a file with *filename* already exists at *repo_path*, it is
backed up as ``{filename}.bak`` so that it can be restored later
(e.g. for the HUMAN baseline condition).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `repo_path` | `Path` | Root of the repository checkout. | *required* |
| `content` | `str` | Content to write. | *required* |
| `filename` | `str` | Target filename (default ``AGENTS.md``). | 'AGENTS.md' |

**Returns:** `Path` - Absolute path to the written file.

#### `remove_context`

```python
def remove_context(self, repo_path: Path, filename: str = 'AGENTS.md') -> None:
```

Remove an injected context file and restore any backup.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `repo_path` | `Path` | Root of the repository checkout. | *required* |
| `filename` | `str` | Target filename (default ``AGENTS.md``). | 'AGENTS.md' |

**Returns:** `None`

### `RedundancyAnalyzer`

Measure token-level redundancy between injected context and docs.

**Methods:**

#### `score_redundancy`

```python
def score_redundancy(self, agents_md: str, repo_docs: list[str]) -> float:
```

Compute average Jaccard similarity across *repo_docs*.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `agents_md` | `str` | The AGENTS.md content to compare. | *required* |
| `repo_docs` | `list[str]` | Existing repository documentation contents. | *required* |

**Returns:** `float` - Average overlap score from 0.0 (fully unique) to 1.0 (fully
    redundant). Returns 0.0 when *repo_docs* is empty.

#### `collect_repo_docs`

```python
def collect_repo_docs(self, repo_path: Path) -> list[str]:
```

Read standard documentation files from a repository.

Reads ``README.md``, ``CONTRIBUTING.md``, ``CLAUDE.md``, and
``AGENTS.md`` when present. Also reads the ``[project.description]``
field from ``pyproject.toml`` if available.

Missing files are silently skipped.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `repo_path` | `Path` |  | *required* |

**Returns:** `list[str]`

#### `analyze_sections`

```python
def analyze_sections(self, agents_md: str, repo_docs: list[str]) -> list[SectionRedundancy]:
```

Score each ``## `` section in *agents_md* against *repo_docs*.

Sections are identified by splitting on lines starting with
``## ``. Each section is scored individually and given a
recommendation:

* ``"remove"`` when redundancy > 0.6
* ``"reduce"`` when redundancy is between 0.3 and 0.6
* ``"keep"`` when redundancy < 0.3

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `agents_md` | `str` |  | *required* |
| `repo_docs` | `list[str]` |  | *required* |

**Returns:** `list[SectionRedundancy]`

---

*Documentation coverage: 90.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.dataset`

Dataset loader for AGENTBench benchmark instances.

Loads benchmark instances from HuggingFace datasets or local
Parquet/JSON/JSONL files, with support for deterministic sampling,
repository filtering, and column-name aliasing.

## Classes

### `DatasetLoadError`(Exception)

Raised when a dataset cannot be loaded.

### `DatasetNotFoundError`(DatasetLoadError)

Raised when the specified dataset does not exist.

### `DependencyMissingError`(DatasetLoadError)

Raised when a required optional dependency is not installed.

### `DatasetLoader`

Load benchmark instances from HuggingFace or local files.

**Methods:**

#### `__init__`

```python
def __init__(self, config: BenchmarkConfig) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `config` | `BenchmarkConfig` |  | *required* |

**Returns:** `None`

#### async `load`

```python
async def load(self) -> list[BenchmarkInstance]:
```

Load benchmark instances based on config.

Dispatches to the appropriate backend based on the dataset
name: local file paths are detected by existence and
extension; everything else is treated as a HuggingFace
dataset identifier.

**Returns:** `list[BenchmarkInstance]` - Loaded (and optionally sampled) list of instances.

**Raises:**

- DatasetLoadError: On any loading failure.
- DatasetNotFoundError: When the dataset is not found.
- DependencyMissingError: When a required library is absent.

#### `sample_subset`

```python
def sample_subset(self, instances: list[BenchmarkInstance], n: int, seed: int | None = None) -> list[BenchmarkInstance]:
```

Deterministic random sampling of instances.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instances` | `list[BenchmarkInstance]` | Full list of instances. | *required* |
| `n` | `int` | Number of instances to sample. | *required* |
| `seed` | `int \| None` | Random seed for reproducibility. | None |

**Returns:** `list[BenchmarkInstance]` - A sampled subset, or the original list when *n* is
    zero or exceeds the collection size.

#### `filter_by_repo`

```python
def filter_by_repo(self, instances: list[BenchmarkInstance], repo_names: list[str]) -> list[BenchmarkInstance]:
```

Filter instances to specific repositories.

Comparison is case-insensitive.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instances` | `list[BenchmarkInstance]` | Full list of instances. | *required* |
| `repo_names` | `list[str]` | Repository names to keep. | *required* |

**Returns:** `list[BenchmarkInstance]` - Filtered list containing only matching repos.

---

*Documentation coverage: 87.5%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.docker_runner`

Docker container management for benchmark evaluation.

## Classes

### `TestResult`

Decorators: `@dataclass`

Result of running tests in a Docker container.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `passed` | `bool` |  |
| `total_tests` | `int` | `0` |
| `passed_tests` | `int` | `0` |
| `failed_tests` | `int` | `0` |
| `stdout` | `str` | `''` |
| `stderr` | `str` | `''` |
| `duration_ms` | `int` | `0` |

### `DockerNotAvailableError`(Exception)

Docker SDK not installed or daemon not running.

### `DockerRunner`

Manages Docker containers for benchmark evaluation.

Uses the Docker SDK for Python to create isolated sandbox containers,
apply patches, run test suites, and clean up resources. Containers are
created with network isolation and memory limits for safety.

**Methods:**

#### `__init__`

```python
def __init__(self, timeout: int = 300) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `timeout` | `int` |  | 300 |

**Returns:** `None`

#### async `prepare_container`

```python
async def prepare_container(self, instance: BenchmarkInstance, work_dir: str | None = None) -> str:
```

Pull/build image and create a container.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instance` | `BenchmarkInstance` | Benchmark instance with Docker image and setup commands. | *required* |
| `work_dir` | `str \| None` | Optional working directory override inside the container. | None |

**Returns:** `str` - The container ID as a string.

#### async `apply_patch`

```python
async def apply_patch(self, container_id: str, patch: str) -> bool:
```

Apply a unified diff patch to the repo in the container.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `container_id` | `str` | Docker container ID. | *required* |
| `patch` | `str` | Unified diff content. | *required* |

**Returns:** `bool` - True if the patch was applied successfully, False otherwise.

#### async `run_tests`

```python
async def run_tests(self, container_id: str, commands: list[str], timeout: int | None = None) -> TestResult:
```

Execute test commands in the container.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `container_id` | `str` | Docker container ID. | *required* |
| `commands` | `list[str]` | Shell commands to execute sequentially. | *required* |
| `timeout` | `int \| None` | Per-command timeout in seconds (defaults to constructor timeout). | None |

**Returns:** `TestResult` - Aggregated test result with stdout/stderr and timing.

#### async `cleanup`

```python
async def cleanup(self, container_id: str) -> None:
```

Remove container and associated volumes.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `container_id` | `str` | Docker container ID to remove. | *required* |

**Returns:** `None`

---

*Documentation coverage: 87.5%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.engagement_calibrator`

Engagement level optimization and calibration.

Evaluates TappsMCP templates at each engagement level (high, medium, low)
plus a no-context baseline, then recommends the level with the best
resolution-per-token efficiency ratio.

## Classes

### `EngagementCalibration`(BaseModel)

Calibration metrics for a single engagement level.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `level` | `str` | `Field(description='Engagement level (high/medium/low/none).')` |
| `resolution_rate` | `float` | `Field(ge=0.0, le=1.0, description='Resolution rate achieved.')` |
| `avg_token_cost` | `float` | `Field(ge=0.0, description='Average token usage per instance.')` |
| `resolution_per_token` | `float` | `Field(ge=0.0, description='Resolution rate divided by avg token cost (efficiency).')` |
| `delta_vs_none` | `float` | `Field(description='Resolution rate improvement over no-context baseline.')` |
| `delta_vs_medium` | `float` | `Field(description='Resolution rate improvement over medium engagement.')` |

### `EngagementCalibrationReport`(BaseModel)

Complete calibration report across all engagement levels.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `calibrations` | `list[EngagementCalibration]` | `Field(description='Per-level calibration results.')` |
| `recommended_level` | `str` | `Field(description='Recommended engagement level.')` |
| `recommendation_reason` | `str` | `Field(description='Explanation for the recommendation.')` |
| `warning` | `str | None` | `Field(default=None, description='Warning message if any level performs worse than no-context.')` |

### `EngagementCalibrator`

Calibrate engagement levels by evaluating each against benchmarks.

**Methods:**

#### async `calibrate`

```python
async def calibrate(self, config: BenchmarkConfig, evaluator: _EvaluatorProtocol) -> EngagementCalibrationReport:
```

Evaluate all engagement levels and recommend the best.

Runs the evaluator for each engagement level (high, medium, low)
plus a no-context baseline, computes efficiency ratios, and
recommends the level with the best resolution-per-token ratio.

Warns if any level performs worse than the no-context baseline.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `config` | `BenchmarkConfig` | Benchmark configuration. | *required* |
| `evaluator` | `_EvaluatorProtocol` | Evaluator (mock or real) for running benchmarks. | *required* |

**Returns:** `EngagementCalibrationReport` - Calibration report with per-level metrics and recommendation.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.evaluator`

Benchmark evaluation orchestrator.

Coordinates running benchmark instances across context modes with
configurable parallelism, progress tracking, and error isolation.

## Classes

### `EvaluatorBackend`(Protocol)

Protocol for evaluation backends (Docker or Mock).

**Methods:**

#### async `evaluate_instance`

```python
async def evaluate_instance(self, instance: BenchmarkInstance, context_mode: ContextMode, engagement_level: str) -> BenchmarkResult:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instance` | `BenchmarkInstance` |  | *required* |
| `context_mode` | `ContextMode` |  | *required* |
| `engagement_level` | `str` |  | *required* |

**Returns:** `BenchmarkResult`

### `Evaluator`

Orchestrates benchmark evaluation across instances and conditions.

Delegates actual evaluation to a backend (Docker or Mock) while
managing parallelism, error handling, and progress reporting.

**Methods:**

#### `__init__`

```python
def __init__(self, config: BenchmarkConfig, backend: EvaluatorBackend | None = None) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `config` | `BenchmarkConfig` |  | *required* |
| `backend` | `EvaluatorBackend \| None` |  | None |

**Returns:** `None`

#### property `injector`

```python
def injector(self) -> ContextInjector:
```

Access the context injector for this evaluator.

**Returns:** `ContextInjector`

#### async `evaluate_instance`

```python
async def evaluate_instance(self, instance: BenchmarkInstance, context_mode: ContextMode) -> BenchmarkResult:
```

Evaluate a single instance with a given context mode.

When a backend is configured, delegates to it. Otherwise returns
an unresolved result with an error message.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instance` | `BenchmarkInstance` | Benchmark instance to evaluate. | *required* |
| `context_mode` | `ContextMode` | Context injection mode. | *required* |

**Returns:** `BenchmarkResult` - Evaluation result for the instance.

#### async `evaluate_batch`

```python
async def evaluate_batch(self, instances: list[BenchmarkInstance], context_mode: ContextMode, progress_callback: Any = None, template_override: str | None = None) -> list[BenchmarkResult]:
```

Evaluate a batch with configurable parallelism.

Individual failures are caught and returned as unresolved results
rather than crashing the entire batch.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instances` | `list[BenchmarkInstance]` | Benchmark instances to evaluate. | *required* |
| `context_mode` | `ContextMode` | Context injection mode. | *required* |
| `progress_callback` | `Any` | Optional callable(completed, total) for progress reporting. | None |
| `template_override` | `str \| None` | Optional template content to use instead of the default generated template (for ablation testing). | None |

**Returns:** `list[BenchmarkResult]` - List of results in the same order as instances.

#### async `evaluate_all_conditions`

```python
async def evaluate_all_conditions(self, instances: list[BenchmarkInstance]) -> dict[ContextMode, list[BenchmarkResult]]:
```

Evaluate instances across all context modes.

When the config context_mode is ALL, evaluates NONE, TAPPS, and
HUMAN. Otherwise evaluates only the specified mode.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instances` | `list[BenchmarkInstance]` | Benchmark instances to evaluate. | *required* |

**Returns:** `dict[ContextMode, list[BenchmarkResult]]` - Mapping from context mode to evaluation results.

---

*Documentation coverage: 75.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.failure_analyzer`

Failure pattern analysis for benchmark results.

Identifies instances that failed with TAPPS context but succeeded
without it or with human context, clusters failures by patterns,
and generates template improvement suggestions.

## Classes

### `FailurePattern`(BaseModel)

A recurring failure pattern identified across benchmark results.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `pattern_type` | `str` | `Field(description="Type of failure pattern (e.g., 'timeout', 'import_error').")` |
| `frequency` | `int` | `Field(ge=0, description='Number of times this pattern occurred.')` |
| `affected_repos` | `list[str]` | `Field(default_factory=list, description='Repositories affected by this pattern.')` |
| `example_instance_ids` | `list[str]` | `Field(default_factory=list, description='Example instance IDs exhibiting this pattern.')` |
| `suggested_fix` | `str` | `Field(description='Suggested template fix for this pattern.')` |

### `TemplateSuggestion`(BaseModel)

A specific suggestion for improving the template.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `section` | `str` | `Field(description='Template section to modify.')` |
| `action` | `str` | `Field(description="Action: 'add', 'modify', or 'remove'.")` |
| `content` | `str` | `Field(description='Content to add or modify.')` |
| `rationale` | `str` | `Field(description='Reason for this suggestion.')` |
| `expected_impact` | `str` | `Field(description='Expected impact on resolution rate.')` |

### `FailureAnalyzer`

Analyze failure patterns in benchmark results.

**Methods:**

#### `analyze_failures`

```python
def analyze_failures(self, results: list[BenchmarkResult], instances: list[BenchmarkInstance]) -> list[FailurePattern]:
```

Identify recurring failure patterns.

Finds instances that failed (not resolved) and clusters them
by error patterns and repositories. Generates suggested fixes
for each pattern (capped at ``_MAX_SUGGESTIONS``).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[BenchmarkResult]` | Benchmark results to analyze. | *required* |
| `instances` | `list[BenchmarkInstance]` | Benchmark instances for context. | *required* |

**Returns:** `list[FailurePattern]` - List of failure patterns sorted by frequency (descending).

#### `generate_suggestions`

```python
def generate_suggestions(self, patterns: list[FailurePattern], template: str) -> list[TemplateSuggestion]:
```

Generate template improvement suggestions from failure patterns.

Maps failure patterns to specific template sections that should
be added or modified.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `patterns` | `list[FailurePattern]` | Failure patterns from ``analyze_failures``. | *required* |
| `template` | `str` | Current template content. | *required* |

**Returns:** `list[TemplateSuggestion]` - List of template suggestions.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.insight_benchmark`

Postgres-backed brain performance benchmark (STORY-102.7).

Benchmarks the tapps-brain MemoryStore (SQLite backend, EPIC-43 Postgres path)
across the core insight operations: bulk write, BM25 search, and bulk_migrate
promotion. Reports p50/p95/p99 latencies per operation so that teams can
baseline SQLite performance and quantify the Postgres uplift once EPIC-43 lands.

Postgres integration
--------------------
The actual Postgres backend lives in EPIC-43 (tapps-brain v3). This benchmark
is intentionally backend-agnostic: it measures whatever store is instantiated.
To benchmark Postgres, instantiate a MemoryStore pointing at a Postgres DSN
and pass it to :func:[`run_insight_benchmark`](#run-insight-benchmark) via the ``store`` parameter.

Typical usage::

    from tapps_mcp.benchmark.insight_benchmark import run_insight_benchmark
    from pathlib import Path

    result = run_insight_benchmark(Path.cwd(), n=500)
    print(result.markdown_report())

## Classes

### `BenchmarkLatencies`(BaseModel)

Latency statistics for a single benchmark operation.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `operation` | `str` | `Field(description='Operation name (write, search, migrate).')` |
| `count` | `int` | `Field(default=0, ge=0, description='Number of samples.')` |
| `p50_ms` | `float` | `Field(default=0.0, description='Median latency (ms).')` |
| `p95_ms` | `float` | `Field(default=0.0, description='95th percentile latency (ms).')` |
| `p99_ms` | `float` | `Field(default=0.0, description='99th percentile latency (ms).')` |
| `mean_ms` | `float` | `Field(default=0.0, description='Mean latency (ms).')` |
| `total_ms` | `float` | `Field(default=0.0, description='Total wall-clock time (ms).')` |
| `throughput_per_s` | `float` | `Field(default=0.0, description='Operations per second.')` |

**Methods:**

#### classmethod `from_samples`

```python
def from_samples(cls, operation: str, samples_ms: list[float]) -> BenchmarkLatencies:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `operation` | `str` |  | *required* |
| `samples_ms` | `list[float]` |  | *required* |

**Returns:** `BenchmarkLatencies`

### `InsightBenchmarkResult`(BaseModel)

Full result of an insight benchmark run.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `backend` | `str` | `Field(default='sqlite', description='Store backend (sqlite or postgres).')` |
| `n` | `int` | `Field(default=0, ge=0, description='Number of synthetic entries used.')` |
| `project_root` | `str` | `Field(default='', description='Project root path.')` |
| `latencies` | `list[BenchmarkLatencies]` | `Field(default_factory=list)` |
| `available` | `bool` | `Field(default=True, description='False when tapps-brain is absent.')` |
| `error` | `str` | `Field(default='', description='Non-empty when benchmark failed.')` |

**Methods:**

#### `markdown_report`

```python
def markdown_report(self) -> str:
```

Return a markdown-formatted benchmark report.

**Returns:** `str`

## Functions

### `run_insight_benchmark`

```python
def run_insight_benchmark(project_root: Path, n: int = 100, *, store: Any = None, backend: str = 'sqlite') -> InsightBenchmarkResult:
```

Run the insight benchmark suite against a MemoryStore.

Phases:
1. **Write** — save N synthetic entries one-by-one; measure per-save latency.
2. **Search** — run 20 BM25 queries; measure per-query latency.
3. **Migrate** — call bulk_migrate on all N entries; measure total latency.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `Path` | Project root for the default MemoryStore. Ignored when *store* is provided. | *required* |
| `n` | `int` | Number of synthetic entries to write. Default 100 for fast CI runs; use 1000+ for production benchmarking. | 100 |
| `store` | `Any` | Optional pre-constructed MemoryStore. When None, opens the default SQLite store at *project_root*. | None |
| `backend` | `str` | Label for the report (``"sqlite"`` or ``"postgres"``). | 'sqlite' |

**Returns:** `InsightBenchmarkResult` - :class:`InsightBenchmarkResult` with per-operation latency stats.

---

*Documentation coverage: 80.0%*
*Quality: 1 function missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.memory_tracker`

Memory subsystem effectiveness tracking for MCP tool benchmarks.

Measures how memory operations (save, get, search, inject) affect task
resolution across different memory tiers (architectural, pattern, procedural, context).

## Classes

### `MemoryEffectiveness`(BaseModel)

Effectiveness metrics for a single memory tier.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `memory_tier` | `str` | `Field(description="Memory tier: 'architectural', 'pattern', 'procedural', or 'context'.")` |
| `retrievals` | `int` | `Field(ge=0, description='Number of memory retrievals observed.')` |
| `resolution_with` | `float` | `Field(ge=0.0, le=1.0, description='Resolution rate when memory was available.')` |
| `resolution_without` | `float` | `Field(ge=0.0, le=1.0, description='Resolution rate when memory was not available.')` |
| `impact` | `float` | `Field(description='Resolution rate delta (with - without).')` |
| `relevance_score` | `float` | `Field(ge=0.0, le=1.0, description='Estimated relevance of retrieved memories.')` |

### `MemoryEffectivenessReport`(BaseModel)

Report summarizing memory effectiveness across tiers.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `per_tier` | `list[MemoryEffectiveness]` | `Field(description='Per-tier effectiveness metrics.')` |
| `most_effective_tier` | `str` | `Field(description='Tier with highest positive impact.')` |

### `MemoryEffectivenessAnalyzer`

Analyze memory subsystem effectiveness from benchmark results.

**Methods:**

#### `analyze`

```python
def analyze(self, results: list[ToolImpactResult]) -> MemoryEffectivenessReport:
```

Analyze memory effectiveness across tiers.

Groups results by whether memory was available, then distributes
tasks across memory tiers using a deterministic assignment based
on task_id hash. This simulates tier-level analysis when actual
tier metadata is not available.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[ToolImpactResult]` | Tool impact results from evaluation. | *required* |

**Returns:** `MemoryEffectivenessReport` - Report with per-tier effectiveness metrics.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.mock_evaluator`

Mock evaluator for benchmark testing without Docker or API access.

## Classes

### `MockEvaluator`

Returns predetermined results for benchmark testing.

Can be configured with specific outcomes per instance, or uses defaults:
- NONE context: 30% resolution rate
- TAPPS context: 45% resolution rate
- HUMAN context: 50% resolution rate

**Methods:**

#### `__init__`

```python
def __init__(self, outcomes: dict[tuple[str, ContextMode], bool] | None = None, default_resolution_rates: dict[ContextMode, float] | None = None, seed: int = 42) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `outcomes` | `dict[tuple[str, ContextMode], bool] \| None` |  | None |
| `default_resolution_rates` | `dict[ContextMode, float] \| None` |  | None |
| `seed` | `int` |  | 42 |

**Returns:** `None`

#### async `evaluate_instance`

```python
async def evaluate_instance(self, instance: BenchmarkInstance, context_mode: ContextMode, engagement_level: str = 'medium') -> BenchmarkResult:
```

Evaluate a single instance with mock results.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instance` | `BenchmarkInstance` |  | *required* |
| `context_mode` | `ContextMode` |  | *required* |
| `engagement_level` | `str` |  | 'medium' |

**Returns:** `BenchmarkResult`

#### async `evaluate_batch`

```python
async def evaluate_batch(self, instances: list[BenchmarkInstance], context_mode: ContextMode, engagement_level: str = 'medium', template_override: str | None = None) -> list[BenchmarkResult]:
```

Evaluate a batch of instances.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instances` | `list[BenchmarkInstance]` |  | *required* |
| `context_mode` | `ContextMode` |  | *required* |
| `engagement_level` | `str` |  | 'medium' |
| `template_override` | `str \| None` |  | None |

**Returns:** `list[BenchmarkResult]`

#### property `call_count`

```python
def call_count(self) -> int:
```

Number of evaluate_instance calls made.

**Returns:** `int`

## Functions

### `make_test_result`

```python
def make_test_result(instance_id: str = 'test-001', context_mode: ContextMode = ContextMode.NONE, resolved: bool = True, **kwargs: Any) -> BenchmarkResult:
```

Factory for creating BenchmarkResult instances in tests.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `instance_id` | `str` |  | 'test-001' |
| `context_mode` | `ContextMode` |  | ContextMode.NONE |
| `resolved` | `bool` |  | True |
| `kwargs` | `Any` |  | *required* |

**Returns:** `BenchmarkResult`

---

*Documentation coverage: 83.3%*
*Quality: 1 function missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.models`

Pydantic v2 models for the benchmark subsystem.

## Classes

### `ContextMode`(StrEnum)

Context injection mode for benchmark runs.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `NONE` |  | `'none'` |
| `TAPPS` |  | `'tapps'` |
| `HUMAN` |  | `'human'` |
| `ALL` |  | `'all'` |

### `BenchmarkInstance`(BaseModel)

A single benchmark instance mirroring the AGENTBench HuggingFace schema.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `instance_id` | `str` | `Field(description='Unique identifier for this instance.')` |
| `repo` | `str` | `Field(description='Repository name (org/repo format).')` |
| `problem_description` | `str` | `Field(description='Natural-language problem statement.')` |
| `clean_pr_patch` | `str` | `Field(description='Gold-standard patch (unified diff).')` |
| `test_commands` | `list[str]` | `Field(description='Commands to run the test suite.')` |
| `test_file_names` | `list[str]` | `Field(description='Paths to test files.')` |
| `test_file_contents` | `dict[str, str]` | `Field(description='Mapping of test file path to file contents.')` |
| `docker_image` | `str` | `Field(description='Docker image for the evaluation sandbox.')` |
| `setup_commands` | `list[str]` | `Field(default_factory=list, description='Commands to prepare the sandbox.')` |
| `key_files` | `list[str]` | `Field(default_factory=list, description='Important files the agent should examine.')` |
| `risk_factors` | `list[str] | None` | `Field(default=None, description='Known risk factors for this instance.')` |
| `rationale` | `str | None` | `Field(default=None, description='Human rationale for expected difficulty.')` |

### `BenchmarkConfig`(BaseModel)

Configuration for a benchmark run.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `dataset_name` | `str` | `Field(default='eth-sri/agentbench', description='HuggingFace dataset ID or local path.')` |
| `dataset_revision` | `str | None` | `Field(default=None, description='HuggingFace dataset revision (commit/tag) to pin for reproducibility.')` |
| `context_mode` | `ContextMode` | `Field(default=ContextMode.NONE, description='Context injection mode.')` |
| `engagement_level` | `str` | `Field(default='medium', description='TappsMCP engagement level (high/medium/low).')` |
| `subset_size` | `int` | `Field(default=20, ge=0, description='Number of instances to sample (0 = all).')` |
| `workers` | `int` | `Field(default=4, ge=1, description='Number of parallel evaluation workers.')` |
| `output_dir` | `Path` | `Field(default=Path('.tapps-mcp/benchmark/'), description='Directory for benchmark output artifacts.')` |
| `docker_timeout` | `int` | `Field(default=300, ge=30, description='Per-instance Docker sandbox timeout in seconds.')` |
| `random_seed` | `int` | `Field(default=42, description='Random seed for reproducible instance sampling.')` |

### `BenchmarkResult`(BaseModel)

Result for a single benchmark instance evaluation.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `instance_id` | `str` | `Field(description='Instance that was evaluated.')` |
| `context_mode` | `ContextMode` | `Field(description='Context mode used.')` |
| `engagement_level` | `str` | `Field(description='Engagement level used.')` |
| `resolved` | `bool` | `Field(description='Whether the agent resolved the issue.')` |
| `token_usage` | `int` | `Field(default=0, ge=0, description='Total tokens consumed.')` |
| `inference_cost` | `float` | `Field(default=0.0, ge=0.0, description='Estimated cost in USD.')` |
| `steps` | `int` | `Field(default=0, ge=0, description='Number of agent steps taken.')` |
| `patch_size` | `int` | `Field(default=0, ge=0, description='Lines changed in the agent patch.')` |
| `error` | `str | None` | `Field(default=None, description='Error message if evaluation failed.')` |
| `duration_ms` | `int` | `Field(default=0, ge=0, description='Wall-clock duration in milliseconds.')` |
| `timestamp` | `str` | `Field(default_factory=_utc_now_iso, description='ISO-8601 timestamp of result creation.')` |

### `RepoBreakdown`(BaseModel)

Per-repository resolution statistics.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `repo` | `str` | `Field(description='Repository name.')` |
| `total` | `int` | `Field(ge=0, description='Total instances for this repo.')` |
| `resolved` | `int` | `Field(ge=0, description='Resolved instances for this repo.')` |

**Methods:**

#### property `resolution_rate`

```python
def resolution_rate(self) -> float:
```

Fraction of resolved instances (0.0-1.0).

**Returns:** `float`

### `BenchmarkSummary`(BaseModel)

Aggregated results for a benchmark run.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `total_instances` | `int` | `Field(ge=0, description='Total instances evaluated.')` |
| `resolved_count` | `int` | `Field(ge=0, description='Total instances resolved.')` |
| `avg_tokens` | `float` | `Field(ge=0.0, description='Mean token usage per instance.')` |
| `avg_cost` | `float` | `Field(ge=0.0, description='Mean inference cost per instance.')` |
| `avg_steps` | `float` | `Field(ge=0.0, description='Mean steps per instance.')` |
| `per_repo_breakdown` | `dict[str, RepoBreakdown]` | `Field(default_factory=dict, description='Per-repository breakdown.')` |
| `context_mode` | `ContextMode` | `Field(description='Context mode for this run.')` |
| `engagement_level` | `str` | `Field(description='Engagement level for this run.')` |

**Methods:**

#### property `resolution_rate`

```python
def resolution_rate(self) -> float:
```

Fraction of resolved instances (0.0-1.0).

**Returns:** `float`

### `ComparisonReport`(BaseModel)

Comparison between a baseline and treatment benchmark run.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `baseline` | `BenchmarkSummary` | `Field(description='Baseline run summary.')` |
| `treatment` | `BenchmarkSummary` | `Field(description='Treatment run summary.')` |
| `resolution_delta` | `float` | `Field(description='Treatment resolution rate minus baseline rate.')` |
| `token_delta` | `float` | `Field(description='Treatment avg tokens minus baseline avg tokens.')` |
| `cost_delta` | `float` | `Field(description='Treatment avg cost minus baseline avg cost.')` |
| `per_repo_deltas` | `dict[str, float]` | `Field(default_factory=dict, description='Per-repository resolution rate deltas.')` |
| `statistically_significant` | `bool | None` | `Field(default=None, description='Whether the delta is statistically significant (None if not computed).')` |
| `p_value` | `float | None` | `Field(default=None, description='P-value from significance test (None if not computed).')` |

### `RunMetadata`(BaseModel)

Metadata for a benchmark run.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `run_id` | `str` | `Field(description='Unique run identifier.')` |
| `timestamp` | `str` | `Field(default_factory=_utc_now_iso, description='ISO-8601 timestamp of run start.')` |
| `config` | `BenchmarkConfig` | `Field(description='Configuration used for the run.')` |
| `instance_count` | `int` | `Field(ge=0, description='Number of instances in this run.')` |
| `context_mode` | `ContextMode` | `Field(description='Context mode for the run.')` |

### `EngagementReport`(BaseModel)

Side-by-side comparison of engagement levels.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `results_by_level` | `dict[str, BenchmarkSummary]` | `Field(description='Per-level results.')` |
| `recommended_level` | `str` | `Field(description='Recommended level.')` |
| `recommendation_reason` | `str` | `Field(description='Why this level is recommended.')` |

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.promotion`

Template promotion gate for version management.

Evaluates whether a candidate template version should be promoted
based on resolution rate improvements, redundancy thresholds,
sample size requirements, and significance thresholds.

## Classes

### `PromotionCriteria`(BaseModel)

Criteria for approving a template version promotion.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `min_resolution_delta` | `float` | `Field(default=0.0, description='Minimum resolution rate improvement over current version.')` |
| `max_redundancy` | `float` | `Field(default=0.5, ge=0.0, le=1.0, description='Maximum acceptable redundancy score.')` |
| `min_instances_evaluated` | `int` | `Field(default=20, ge=1, description='Minimum number of instances that must be evaluated.')` |
| `significance_threshold` | `float` | `Field(default=0.1, ge=0.0, description='Minimum absolute resolution delta to consider meaningful.')` |

### `PromotionDecision`(BaseModel)

Decision result from promotion evaluation.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `approved` | `bool` | `Field(description='Whether the promotion is approved.')` |
| `reason` | `str` | `Field(description='Explanation for the decision.')` |
| `warnings` | `list[str]` | `Field(default_factory=list, description='Non-blocking warnings about the candidate.')` |

## Functions

### `evaluate_promotion`

```python
def evaluate_promotion(candidate: TemplateVersion, current: TemplateVersion | None, criteria: PromotionCriteria) -> PromotionDecision:
```

Evaluate whether a candidate version should be promoted.

Checks the candidate against the promotion criteria:
1. Candidate must have benchmark scores
2. Enough instances must have been evaluated
3. Resolution rate must improve over current (if present)
4. Redundancy must be within threshold
5. Improvement must be above significance threshold

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `candidate` | `TemplateVersion` | Candidate template version to evaluate. | *required* |
| `current` | `TemplateVersion \| None` | Currently promoted version (``None`` if first promotion). | *required* |
| `criteria` | `PromotionCriteria` | Promotion criteria to apply. | *required* |

**Returns:** `PromotionDecision` - Promotion decision with approval status and reasoning.

### `auto_promote`

```python
def auto_promote(candidate_version: int, store: TemplateVersionStore) -> bool:
```

Automatically evaluate and promote a candidate version.

Looks up the candidate version, finds the current best for the
same engagement level, evaluates against default criteria, and
promotes if approved.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `candidate_version` | `int` | Version number of the candidate. | *required* |
| `store` | `TemplateVersionStore` | Template version store. | *required* |

**Returns:** `bool` - ``True`` if the version was promoted, ``False`` otherwise.

---

*Documentation coverage: 100.0%*
*Quality: 2 functions missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.redundancy`

Enhanced redundancy analysis with per-section scoring and TF-IDF.

Provides `[`RedundancyAnalyzerV2`](#redundancyanalyzerv2)` which computes TF-IDF cosine similarity
combined with token-level Jaccard overlap to score template sections
against existing repository documentation. Generates reduced templates
by removing or trimming redundant sections.

## Classes

### `SectionRedundancyReport`(BaseModel)

Per-section redundancy analysis result.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `section_name` | `str` | `Field(description='Name of the template section.')` |
| `section_content` | `str` | `Field(description='Full content of the section.')` |
| `redundancy_score` | `float` | `Field(ge=0.0, le=1.0, description='Combined redundancy score (0.0-1.0).')` |
| `overlapping_sources` | `list[str]` | `Field(default_factory=list, description='Source documents with overlap.')` |
| `recommendation` | `str` | `Field(description="Action: 'keep', 'reduce', or 'remove'.")` |
| `unique_content` | `str` | `Field(default='', description='Content unique to this section.')` |

### `TemplateRedundancyReport`(BaseModel)

Full template redundancy analysis result.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `overall_score` | `float` | `Field(ge=0.0, le=1.0, description='Average redundancy across sections.')` |
| `sections` | `list[SectionRedundancyReport]` | `Field(default_factory=list, description='Per-section reports.')` |
| `total_sections` | `int` | `Field(ge=0, description='Total sections analyzed.')` |
| `sections_to_remove` | `int` | `Field(ge=0, description='Sections recommended for removal.')` |
| `sections_to_reduce` | `int` | `Field(ge=0, description='Sections recommended for reduction.')` |

### `RedundancyAnalyzerV2`

Enhanced redundancy analyzer with TF-IDF and Jaccard scoring.

**Methods:**

#### `analyze_template_redundancy`

```python
def analyze_template_redundancy(self, template: str, repo_path: Path) -> TemplateRedundancyReport:
```

Analyze template redundancy against repository documentation.

For each section in the template, computes a combined score
using TF-IDF cosine similarity (60%) and token-level Jaccard
(40%) against the collected repo docs.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `template` | `str` | Full template content. | *required* |
| `repo_path` | `Path` | Path to the repository root. | *required* |

**Returns:** `TemplateRedundancyReport` - Comprehensive redundancy report with per-section analysis.

#### `generate_reduced_template`

```python
def generate_reduced_template(self, report: TemplateRedundancyReport) -> str:
```

Generate a reduced template based on redundancy analysis.

Removes sections marked ``"remove"`` and strips overlapping
content from sections marked ``"reduce"``. Preserves ``"keep"``
sections verbatim and maintains markdown structure.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `report` | `TemplateRedundancyReport` | Redundancy analysis report. | *required* |

**Returns:** `str` - Reduced template string.

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.reporter`

Persistence and reporting for benchmark results.

`[`ResultsPersistence`](#resultspersistence)` saves and loads benchmark run data (JSONL results,
CSV summary, JSON metadata).  `[`ReportGenerator`](#reportgenerator)` produces human-readable
Markdown and CSV reports from comparison and result data.

## Classes

### `ResultsPersistence`

Save and load benchmark results to/from disk.

Results are stored under ``{output_dir}/{run_id}/`` with three
files: ``results.jsonl``, ``summary.csv``, and ``metadata.json``.

**Methods:**

#### `__init__`

```python
def __init__(self, output_dir: Path) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `output_dir` | `Path` |  | *required* |

**Returns:** `None`

#### `save_results`

```python
def save_results(self, results: list[BenchmarkResult], run_id: str, config: BenchmarkConfig | None = None) -> Path:
```

Persist results, CSV summary, and metadata for a run.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[BenchmarkResult]` | List of benchmark results to save. | *required* |
| `run_id` | `str` | Unique identifier for this run. | *required* |
| `config` | `BenchmarkConfig \| None` | Optional config to embed in metadata. | None |

**Returns:** `Path` - Path to the run directory.

#### `load_results`

```python
def load_results(self, run_id: str) -> list[BenchmarkResult]:
```

Load results from a saved run.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `run_id` | `str` | Run identifier to load. | *required* |

**Returns:** `list[BenchmarkResult]` - List of ``BenchmarkResult`` parsed from JSONL.

**Raises:**

- FileNotFoundError: If the run directory or JSONL file does not exist.

#### `list_runs`

```python
def list_runs(self) -> list[RunMetadata]:
```

List all saved runs, sorted newest first.

Scans the output directory for subdirectories containing
``metadata.json``.

**Returns:** `list[RunMetadata]` - List of ``RunMetadata`` sorted by timestamp descending.

### `ReportGenerator`

Generate human-readable reports from benchmark data.

**Methods:**

#### `generate_markdown`

```python
def generate_markdown(self, comparison: ComparisonReport) -> str:
```

Generate a Markdown comparison report.

Includes header, summary, resolution rates, token/cost
comparison, per-repo breakdown, and statistical significance.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `comparison` | `ComparisonReport` | Comparison report to render. | *required* |

**Returns:** `str` - Markdown-formatted string.

#### `generate_csv`

```python
def generate_csv(self, results: list[BenchmarkResult]) -> str:
```

Generate a CSV string from benchmark results.

Columns: instance_id, context_mode, resolved, token_usage,
inference_cost, steps, duration_ms.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[BenchmarkResult]` | Results to export. | *required* |

**Returns:** `str` - CSV-formatted string with header row.

---

*Documentation coverage: 87.5%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.template_versions`

Template version tracking with SQLite persistence.

Records template versions, their benchmark scores, redundancy metrics,
and promotion status. Enables comparing template iterations and
selecting the best-performing version for each engagement level.

## Classes

### `TemplateVersion`(BaseModel)

A versioned snapshot of a template with optional benchmark scores.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `version` | `int` | `Field(description='Auto-incremented version number.')` |
| `content_hash` | `str` | `Field(description='SHA-256 hash of the template content.')` |
| `engagement_level` | `str` | `Field(description='Engagement level (high/medium/low).')` |
| `benchmark_scores` | `BenchmarkSummary | None` | `Field(default=None, description='Benchmark results for this version.')` |
| `redundancy_score` | `float | None` | `Field(default=None, description='Overall redundancy score (0.0-1.0).')` |
| `section_scores` | `dict[str, float] | None` | `Field(default=None, description='Per-section redundancy scores.')` |
| `created_at` | `str` | `Field(description='ISO-8601 creation timestamp.')` |
| `promoted` | `bool` | `Field(default=False, description='Whether this version is promoted.')` |
| `promotion_reason` | `str | None` | `Field(default=None, description='Reason for promotion.')` |
| `metadata` | `dict[str, str]` | `Field(default_factory=dict, description='Arbitrary key-value metadata.')` |

### `TemplateVersionStore`

SQLite-backed store for template version tracking.

**Methods:**

#### `__init__`

```python
def __init__(self, db_path: Path) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `db_path` | `Path` |  | *required* |

**Returns:** `None`

#### `close`

```python
def close(self) -> None:
```

Close the database connection.

**Returns:** `None`

#### `record_version`

```python
def record_version(self, content: str, engagement_level: str, metadata: dict[str, str] | None = None) -> TemplateVersion:
```

Record a new template version.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `content` | `str` | Full template content. | *required* |
| `engagement_level` | `str` | Engagement level for this template. | *required* |
| `metadata` | `dict[str, str] \| None` | Optional key-value metadata. | None |

**Returns:** `TemplateVersion` - The newly created ``TemplateVersion``.

#### `record_scores`

```python
def record_scores(self, version: int, benchmark_scores: BenchmarkSummary | None = None, redundancy_score: float | None = None, section_scores: dict[str, float] | None = None) -> None:
```

Record benchmark and redundancy scores for a version.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `version` | `int` | Version number to update. | *required* |
| `benchmark_scores` | `BenchmarkSummary \| None` | Benchmark summary results. | None |
| `redundancy_score` | `float \| None` | Overall redundancy score. | None |
| `section_scores` | `dict[str, float] \| None` | Per-section redundancy scores. | None |

**Returns:** `None`

#### `get_latest`

```python
def get_latest(self, engagement_level: str) -> TemplateVersion | None:
```

Get the latest version for an engagement level.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` | Engagement level to query. | *required* |

**Returns:** `TemplateVersion | None` - Latest version or ``None`` if none exist.

#### `get_history`

```python
def get_history(self, engagement_level: str, limit: int = 10) -> list[TemplateVersion]:
```

Get version history for an engagement level.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` | Engagement level to query. | *required* |
| `limit` | `int` | Maximum number of versions to return. | 10 |

**Returns:** `list[TemplateVersion]` - List of versions, newest first.

#### `get_best`

```python
def get_best(self, engagement_level: str) -> TemplateVersion | None:
```

Get the version with the highest resolution rate.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `engagement_level` | `str` | Engagement level to query. | *required* |

**Returns:** `TemplateVersion | None` - Best version or ``None`` if none have benchmark scores.

#### `promote`

```python
def promote(self, version: int, reason: str) -> bool:
```

Mark a version as promoted.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `version` | `int` | Version number to promote. | *required* |
| `reason` | `str` | Reason for promotion. | *required* |

**Returns:** `bool` - ``True`` if the version was found and promoted.

#### `compare`

```python
def compare(self, version_a: int, version_b: int) -> dict[str, Any]:
```

Compare two template versions.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `version_a` | `int` | First version number. | *required* |
| `version_b` | `int` | Second version number. | *required* |

**Returns:** `dict[str, Any]` - Dictionary with comparison metrics.

---

*Documentation coverage: 90.9%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.tool_evaluator`

Tool impact measurement and effectiveness evaluation.

Evaluates how individual MCP tools affect task resolution by running
tasks under different tool availability conditions (all tools, no tools,
all-minus-one) and computing per-tool impact rankings.

## Classes

### `ToolCondition`(StrEnum)

Tool availability condition for evaluation.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `ALL_TOOLS` |  | `'all_tools'` |
| `NO_TOOLS` |  | `'no_tools'` |
| `SINGLE_TOOL` |  | `'single_tool'` |
| `ALL_MINUS_ONE` |  | `'all_minus_one'` |

### `ToolImpactResult`(BaseModel)

Result from evaluating a task under a specific tool condition.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `task_id` | `str` | `Field(description='Task that was evaluated.')` |
| `condition` | `ToolCondition` | `Field(description='Tool availability condition.')` |
| `tool_name` | `str | None` | `Field(default=None, description='Tool that was removed or isolated.')` |
| `resolved` | `bool` | `Field(description='Whether the task was resolved.')` |
| `tools_called` | `list[str]` | `Field(default_factory=list, description='Tools that were called during evaluation.')` |
| `call_count` | `int` | `Field(default=0, ge=0, description='Total tool calls made.')` |
| `token_usage` | `int` | `Field(default=0, ge=0, description='Total tokens consumed.')` |
| `duration_ms` | `int` | `Field(default=0, ge=0, description='Duration in milliseconds.')` |

### `ToolRanking`(BaseModel)

Aggregated ranking for a single tool's effectiveness.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `tool_name` | `str` | `Field(description='Name of the tool.')` |
| `impact_score` | `float` | `Field(description='Aggregate impact score (higher is better).')` |
| `tasks_helped` | `int` | `Field(ge=0, description='Tasks where removing the tool hurt resolution.')` |
| `tasks_hurt` | `int` | `Field(ge=0, description='Tasks where removing the tool helped resolution.')` |
| `tasks_neutral` | `int` | `Field(ge=0, description='Tasks unaffected by tool removal.')` |
| `avg_token_cost` | `int` | `Field(description='Signed average token delta (with-tool minus without-tool). Positive = the tool added tokens; Negative = the tool saved tokens (the with-tool run was cheaper). TAP-1799: the field is signed — earlier versions clamped to >=0 which made cost-saving tools indistinguishable from no-effect ones.')` |
| `pass_at_k` | `float | None` | `Field(default=None, ge=0.0, le=1.0, description='Pass@k metric (fraction of tasks helped).')` |

### `ToolEffectivenessReport`(BaseModel)

Report summarizing tool effectiveness across all tasks.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `tool_rankings` | `list[ToolRanking]` | `Field(description='Tools ranked by impact score (descending).')` |
| `task_count` | `int` | `Field(ge=0, description='Total tasks evaluated.')` |
| `condition_count` | `int` | `Field(ge=0, description='Total conditions evaluated.')` |

### `MockToolEvaluator`

Simulates tool availability effects on task resolution.

Uses deterministic hashing to produce consistent results across runs.
Tool availability affects resolution probability based on whether the
task's expected tools overlap with available tools.

**Methods:**

#### `__init__`

```python
def __init__(self, seed: int = 42) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `seed` | `int` |  | 42 |

**Returns:** `None`

#### async `evaluate_task`

```python
async def evaluate_task(self, task: ToolTask, available_tools: list[str]) -> ToolTaskResult:
```

Evaluate a task with a specific set of available tools.

Resolution probability is determined by how many of the task's
expected tools are available. Full overlap gives ~80% resolution;
no overlap gives ~20%.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `task` | `ToolTask` |  | *required* |
| `available_tools` | `list[str]` |  | *required* |

**Returns:** `ToolTaskResult`

#### property `call_count`

```python
def call_count(self) -> int:
```

Total evaluate_task calls made.

**Returns:** `int`

### `ToolImpactEvaluator`

Evaluate the impact of individual MCP tools on task resolution.

Runs tasks under different tool availability conditions and computes
per-tool impact rankings based on resolution deltas.

**Methods:**

#### `__init__`

```python
def __init__(self, tasks: list[ToolTask] | None = None) -> None:
```

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tasks` | `list[ToolTask] \| None` |  | None |

**Returns:** `None`

#### property `tasks`

```python
def tasks(self) -> list[ToolTask]:
```

Tasks used for evaluation.

**Returns:** `list[ToolTask]`

#### async `evaluate_tool_impact`

```python
async def evaluate_tool_impact(self, tool_name: str, evaluator: MockToolEvaluator) -> list[ToolImpactResult]:
```

Evaluate the impact of a single tool across all tasks.

For each task, runs with ALL_TOOLS and ALL_MINUS_ONE (removing
tool_name) to measure the delta in resolution.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tool_name` | `str` | The tool to measure impact for. | *required* |
| `evaluator` | `MockToolEvaluator` | Mock evaluator to simulate tool effects. | *required* |

**Returns:** `list[ToolImpactResult]` - Paired results (all_tools, all_minus_one) for each task.

#### async `evaluate_all_tools`

```python
async def evaluate_all_tools(self, tools: list[str], evaluator: MockToolEvaluator) -> ToolEffectivenessReport:
```

Evaluate impact of all specified tools.

For each tool, runs paired evaluation across all tasks, then
ranks tools by aggregate impact score.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tools` | `list[str]` | List of tool names to evaluate. | *required* |
| `evaluator` | `MockToolEvaluator` | Mock evaluator to simulate tool effects. | *required* |

**Returns:** `ToolEffectivenessReport` - Report with tool rankings ordered by impact score.

#### `compute_ranking`

```python
def compute_ranking(self, results: list[ToolImpactResult]) -> list[ToolRanking]:
```

Compute per-tool rankings from paired evaluation results.

Groups results by tool_name, then compares ALL_TOOLS vs
ALL_MINUS_ONE outcomes per task to determine if the tool
helped, hurt, or was neutral.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `results` | `list[ToolImpactResult]` | Paired ToolImpactResult entries. | *required* |

**Returns:** `list[ToolRanking]` - Rankings sorted by impact_score descending.

---

*Documentation coverage: 85.7%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.tool_report`

Dashboard integration and reporting for tool effectiveness benchmarks.

Generates human-readable reports from tool effectiveness, call pattern,
and calibration data. Provides a ToolEffectivenessSection model for
integration with the main TappsMCP dashboard.

## Classes

### `ToolEffectivenessSection`(BaseModel)

Tool effectiveness section for the main dashboard.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `tool_rankings` | `list[ToolRanking]` | `Field(description='Tools ranked by effectiveness.')` |
| `call_efficiency` | `float` | `Field(ge=0.0, le=1.0, description='Average call efficiency across tasks.')` |
| `calibration_pending` | `bool` | `Field(default=False, description='Whether checklist calibration needs attention.')` |

## Functions

### `generate_tool_effectiveness_report`

```python
def generate_tool_effectiveness_report(tool_report: ToolEffectivenessReport, call_report: CallPatternReport, output_format: str = 'markdown') -> str:
```

Generate a tool effectiveness report in the specified format.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tool_report` | `ToolEffectivenessReport` | Tool effectiveness report from benchmark. | *required* |
| `call_report` | `CallPatternReport` | Call pattern analysis from benchmark. | *required* |
| `output_format` | `str` | Output format - 'markdown' or 'json'. | 'markdown' |

**Returns:** `str` - Formatted report string.

---

*Documentation coverage: 100.0%*
*Quality: 1 function missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.benchmark.tool_task_models`

MCPMark-inspired tool evaluation task definitions.

Defines deterministic tasks that measure the effectiveness of individual
MCP tools. Each task includes setup files with known issues, expected
tool calls, and verification criteria.

## Classes

### `ToolTaskVerification`(BaseModel)

Verification criteria for a tool task.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `check_type` | `str` | `Field(description="Type of verification check: 'file_content', 'score_threshold', 'test_pass', or 'no_security_issues'.")` |
| `expected_file` | `str | None` | `Field(default=None, description='File path to check for content verification.')` |
| `expected_content_patterns` | `list[str] | None` | `Field(default=None, description='Regex patterns that the output file must match.')` |
| `min_quality_score` | `float | None` | `Field(default=None, ge=0.0, le=100.0, description='Minimum quality score threshold (0-100).')` |
| `custom_verify` | `str | None` | `Field(default=None, description='Custom verification expression or description.')` |

### `ToolTask`(BaseModel)

A single tool evaluation task with setup and verification.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `task_id` | `str` | `Field(description='Unique identifier for this task.')` |
| `category` | `str` | `Field(description="Task category: 'quality', 'security', 'architecture', 'debugging', or 'refactoring'.")` |
| `description` | `str` | `Field(description='Human-readable task description.')` |
| `setup_files` | `dict[str, str]` | `Field(description='Mapping of filename to file content for task setup.')` |
| `expected_tools` | `list[str]` | `Field(description='List of MCP tools an ideal agent would call.')` |
| `verification` | `ToolTaskVerification` | `Field(description='Criteria for verifying task completion.')` |
| `difficulty` | `str` | `Field(description="Task difficulty: 'easy', 'medium', or 'hard'.")` |

### `ToolTaskResult`(BaseModel)

Result from running a single tool task under specific conditions.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(frozen=True)` |
| `task_id` | `str` | `Field(description='Task that was evaluated.')` |
| `condition` | `str` | `Field(description="Evaluation condition: 'all_tools', 'no_tools', 'single_tool', or 'all_minus_one'.")` |
| `tool_name` | `str | None` | `Field(default=None, description='Tool name relevant to the condition (e.g. removed or isolated tool).')` |
| `resolved` | `bool` | `Field(description='Whether the task was resolved successfully.')` |
| `tools_called` | `list[str]` | `Field(default_factory=list, description='Tools that were actually called during evaluation.')` |
| `call_count` | `int` | `Field(default=0, ge=0, description='Total number of tool calls made.')` |
| `token_usage` | `int` | `Field(default=0, ge=0, description='Total tokens consumed during evaluation.')` |
| `duration_ms` | `int` | `Field(default=0, ge=0, description='Wall-clock duration in milliseconds.')` |

## Constants

| Name | Type | Value |
|------|------|-------|
| `BUILTIN_TASKS` | `list[ToolTask]` | `_build_builtin_tasks()` |

---

*Documentation coverage: 100.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.cli`

CLI entry point for tapps-mcp.

## Functions

### `main`

```python
def main() -> None:
```

TappsMCP: MCP server providing code quality tools.

**Returns:** `None`

### `fleet`

```python
def fleet() -> None:
```

Manage the host-level shared HTTP MCP fleet (ADR-0024).

**Returns:** `None`

### `fleet_start`

```python
def fleet_start(force: bool) -> None:
```

Start six long-lived HTTP NLT servers on localhost:8760-8765.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `force` | `bool` |  | *required* |

**Returns:** `None`

### `fleet_stop`

```python
def fleet_stop() -> None:
```

Stop all HTTP fleet servers.

**Returns:** `None`

### `fleet_status_cmd`

```python
def fleet_status_cmd() -> None:
```

Show HTTP fleet process and reachability status.

**Returns:** `None`

### `fleet_restart`

```python
def fleet_restart(force: bool) -> None:
```

Stop then start the HTTP fleet.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `force` | `bool` |  | *required* |

**Returns:** `None`

### `fleet_ensure`

```python
def fleet_ensure() -> None:
```

Restart the fleet only when it is not fully reachable (watchdog entry).

**Returns:** `None`

### `fleet_install_systemd`

```python
def fleet_install_systemd() -> None:
```

Install systemd user units for the fleet + health-aware watchdog timer.

**Returns:** `None`

### `serve`

```python
def serve(transport: str, host: str, port: int, mode: str, tool_profile: str | None) -> None:
```

Start the TappsMCP MCP server.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `transport` | `str` |  | *required* |
| `host` | `str` |  | *required* |
| `port` | `int` |  | *required* |
| `mode` | `str` |  | *required* |
| `tool_profile` | `str \| None` |  | *required* |

**Returns:** `None`

**Examples:**

```python
def test_combined_server_name(combined) -> None:
    """Combined server is named TappsPlatform."""
    assert combined.name == "TappsPlatform"
```

### `init`

```python
def init(mcp_host: str, project_root: str, check: bool, force: bool, scope: str, rules: bool, dry_run: bool, engagement_level: str | None, overwrite_tech_stack: bool, allow_package_init: bool, with_docs_mcp: bool, mcp_bundle: str, legacy_monolith: bool, uv_flag: bool | None, uv_extra: str | None, with_context7: str | None, mcp_transport: str | None) -> None:
```

Bootstrap TappsMCP in a project (MCP config, AGENTS.md, hooks, agents, skills, rules).

Creates or merges `.tapps-mcp.yaml` (including `memory_hooks` when engagement implies it).
Memory pipeline defaults (auto-save, recurring quick_check, architectural supersede, hooks)
come from shipped `default.yaml` unless your YAML overrides them — see docs/MEMORY_REFERENCE.md.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `mcp_host` | `str` |  | *required* |
| `project_root` | `str` |  | *required* |
| `check` | `bool` |  | *required* |
| `force` | `bool` |  | *required* |
| `scope` | `str` |  | *required* |
| `rules` | `bool` |  | *required* |
| `dry_run` | `bool` |  | *required* |
| `engagement_level` | `str \| None` |  | *required* |
| `overwrite_tech_stack` | `bool` |  | *required* |
| `allow_package_init` | `bool` |  | *required* |
| `with_docs_mcp` | `bool` |  | *required* |
| `mcp_bundle` | `str` |  | *required* |
| `legacy_monolith` | `bool` |  | *required* |
| `uv_flag` | `bool \| None` |  | *required* |
| `uv_extra` | `str \| None` |  | *required* |
| `with_context7` | `str \| None` |  | *required* |
| `mcp_transport` | `str \| None` |  | *required* |

**Returns:** `None`

### `upgrade`

```python
def upgrade(mcp_host: str, project_root: str, force: bool, dry_run: bool, scope: str, emit_json: bool) -> None:
```

Refresh generated files after upgrading the `tapps-mcp` package.

Re-merges AGENTS.md, platform rules, hooks, agents, skills, and Claude/Cursor settings.
Creates a timestamped backup under `.tapps-mcp/backups/` before overwriting.
Preserves custom MCP command paths. Review `.tapps-mcp.yaml` after major upgrades if you
relied on older default flags (memory pipeline, hooks). See docs/UPGRADE_FOR_CONSUMERS.md.

v3.11.0 surfaces: refreshed `tapps-stop.sh` (warn-mode completion-gate telemetry on every
project), new `tapps-upgrade` skill in `.claude/skills/` and `.cursor/skills/`,
and DEPRECATED notices on wrapper skills (tapps-score, tapps-gate, tapps-validate, tapps-report)
scheduled for removal in v3.12.0.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `mcp_host` | `str` |  | *required* |
| `project_root` | `str` |  | *required* |
| `force` | `bool` |  | *required* |
| `dry_run` | `bool` |  | *required* |
| `scope` | `str` |  | *required* |
| `emit_json` | `bool` |  | *required* |

**Returns:** `None`

### `session_end_cmd`

```python
def session_end_cmd(project_root: str) -> None:
```

Close the TAPPS session lifecycle (flywheel + session search).

Best-effort mirror of ``tapps_session_end`` for hosts without MCP wiring.
Always exits 0 — brain outages are reported in the output, not as errors.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |

**Returns:** `None`

### `handoff`

```python
def handoff() -> None:
```

Cross-session handoff utilities.

**Returns:** `None`

### `handoff_write`

```python
def handoff_write(file_path: str | None, project_root: str, no_brain_mirror: bool, with_session_end: bool, allow_lint_warnings: bool) -> None:
```

Atomically write session handoff file, mirror to brain, and lint schema.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `file_path` | `str \| None` |  | *required* |
| `project_root` | `str` |  | *required* |
| `no_brain_mirror` | `bool` |  | *required* |
| `with_session_end` | `bool` |  | *required* |
| `allow_lint_warnings` | `bool` |  | *required* |

**Returns:** `None`

### `doctor`

```python
def doctor(project_root: str, quick: bool) -> None:
```

Diagnose MCP config, bootstrap files, hooks, checkers, tapps-brain, and memory flags.

Includes an informational **Memory pipeline (effective config)** row (resolved settings).
Since v3.11.0 also reports `completion_gate_hook` (warns when ``.claude/hooks/tapps-stop.sh``
is missing so warn-mode telemetry to ``.completion-gate-violations.jsonl`` is inactive)
and a `usage_gaps` summary (gap count + top recommendation from ``tapps_usage``).

Use `--quick` to skip per-tool version probes.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |
| `quick` | `bool` |  | *required* |

**Returns:** `None`

### `usage_gaps_hint_cmd`

```python
def usage_gaps_hint_cmd(project_root: str) -> None:
```

Print a one-line prior-session pipeline reminder for SessionStart hooks (TAP-3578).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |

**Returns:** `None`

### `audit_fleet_cmd`

```python
def audit_fleet_cmd(period: str, roots: str, scan_parent: str, output_format: str, no_brain: bool) -> None:
```

Audit TAPPS usage across bootstrapped projects (local JSONL + brain merge).

Discovers projects via ``--roots``, ``TAPPS_FLEET_ROOTS``, or by scanning
``--scan-parent`` for ``.tapps-mcp.yaml`` markers.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `period` | `str` |  | *required* |
| `roots` | `str` |  | *required* |
| `scan_parent` | `str` |  | *required* |
| `output_format` | `str` |  | *required* |
| `no_brain` | `bool` |  | *required* |

**Returns:** `None`

### `loop_metrics_record_cmd`

```python
def loop_metrics_record_cmd() -> None:
```

Record loop-metrics from Cursor/Claude stop-hook stdin (TAP-3918).

**Returns:** `None`

### `tool_usage_fleet_cmd`

```python
def tool_usage_fleet_cmd(period: str, roots: str, scan_parent: str, output_format: str, no_brain: bool) -> None:
```

Per-tool fleet usage leaderboard (TAP-3919).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `period` | `str` |  | *required* |
| `roots` | `str` |  | *required* |
| `scan_parent` | `str` |  | *required* |
| `output_format` | `str` |  | *required* |
| `no_brain` | `bool` |  | *required* |

**Returns:** `None`

### `deploy_local_cmd`

```python
def deploy_local_cmd(tapps_checkout: str, dry_run: bool, skip_gate: bool, force_build: bool, keep_releases: int, skip_doctor_smoke: bool) -> None:
```

Blue/green deploy dev-monorepo MCP CLIs to ~/.tapps-mcp/current.

Builds an immutable release venv, smoke-tests it, atomically flips the
``current`` symlink, and GCs old releases. Running MCP servers stay pinned
to their release dir; reload MCP in Cursor to pick up the new build.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tapps_checkout` | `str` |  | *required* |
| `dry_run` | `bool` |  | *required* |
| `skip_gate` | `bool` |  | *required* |
| `force_build` | `bool` |  | *required* |
| `keep_releases` | `int` |  | *required* |
| `skip_doctor_smoke` | `bool` |  | *required* |

**Returns:** `None`

### `upgrade_fleet_cmd`

```python
def upgrade_fleet_cmd(roots: str, scan_parent: str, mcp_bundle: str, uv_mode: str, mcp_host: str, reinstall_clis: bool, blue_green_deploy: bool, force_inplace_cli_reinstall: bool, tapps_checkout: str, skip_mcp_refresh: bool, skip_doctor: bool, dry_run: bool, force: bool, import_legacy_doc_cache: bool, strip_context7_env: bool, output_format: str) -> None:
```

Upgrade TAPPS scaffolding + NLT MCP config across bootstrapped projects.

Discovers repos via ``--roots``, ``TAPPS_FLEET_ROOTS``, ``--scan-parent``,
or the maintainer default list. Migrates legacy ``tapps-mcp`` + ``docs-mcp``
monolith entries to NLT ``nlt-*`` servers on ``upgrade --force``.

Set fleet roots once::

export TAPPS_FLEET_ROOTS=\
~/code/AgentForge,~/code/NLTlabsPE,~/code/ReportLab,~/code/tapps-mcp

Then::

tapps-mcp upgrade-fleet --reinstall-clis --bundle full --uv-mode off

Opt into blue/green zero-downtime deploy (ADR-0019) with ``--blue-green-deploy``.
When live MCP `[`serve`](#serve)` processes are detected, in-place reinstall auto-promotes
to blue/green unless ``--force-inplace-cli-reinstall`` is set.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `roots` | `str` |  | *required* |
| `scan_parent` | `str` |  | *required* |
| `mcp_bundle` | `str` |  | *required* |
| `uv_mode` | `str` |  | *required* |
| `mcp_host` | `str` |  | *required* |
| `reinstall_clis` | `bool` |  | *required* |
| `blue_green_deploy` | `bool` |  | *required* |
| `force_inplace_cli_reinstall` | `bool` |  | *required* |
| `tapps_checkout` | `str` |  | *required* |
| `skip_mcp_refresh` | `bool` |  | *required* |
| `skip_doctor` | `bool` |  | *required* |
| `dry_run` | `bool` |  | *required* |
| `force` | `bool` |  | *required* |
| `import_legacy_doc_cache` | `bool` |  | *required* |
| `strip_context7_env` | `bool` |  | *required* |
| `output_format` | `str` |  | *required* |

**Returns:** `None`

### `check_agents_md_stamp`

```python
def check_agents_md_stamp(project_root: str) -> None:
```

Release gate — exit 1 if AGENTS.md version marker != pyproject version (TAP-982).

Minimal, single-purpose check suitable for release CI. Faster than a full
`[`doctor`](#doctor)` run and reports only the stamp-vs-package comparison so the
failure message is unambiguous.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |

**Returns:** `None`

### `auto_capture`

```python
def auto_capture(project_root: str, max_facts: int) -> None:
```

Extract durable facts from stdin (Stop hook JSON) and save to memory (Epic 65.5).

Read JSON from stdin (Claude Code Stop event), extract decision-like facts,
and save to project memory. Invoked by memory_auto_capture Stop hook.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |
| `max_facts` | `int` |  | *required* |

**Returns:** `None`

### `compact_index_cmd`

```python
def compact_index_cmd(project_root: str) -> None:
```

Index pre-compaction session state in brain (PreCompact hook, TAP-2017).

Read JSON from stdin (Claude Code PreCompact event), index the session
context via memory_index_session, and write a compaction marker so
tapps_session_start can surface prior session context on rehydration.

Disabled by setting TAPPS_MCP_COMPACTION_REHYDRATE=false.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |

**Returns:** `None`

### `validate_changed_cmd`

```python
def validate_changed_cmd(quick: bool, file_paths: str, project_root: str) -> None:
```

Validate changed Python files (same logic as the MCP tool).

Run this before ending a session to confirm changed files pass quality gates.
Without --file-paths, uses git to detect changed files, then runs quick
(ruff-only) or full (ruff + mypy + bandit + radon + vulture) checks per file.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `quick` | `bool` |  | *required* |
| `file_paths` | `str` |  | *required* |
| `project_root` | `str` |  | *required* |

**Returns:** `None`

### `quick_check_cmd`

```python
def quick_check_cmd(file_path: str, preset: str, project_root: str) -> None:
```

Quick score + gate + security for one file (MCP tapps_quick_check equivalent).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `file_path` | `str` |  | *required* |
| `preset` | `str` |  | *required* |
| `project_root` | `str` |  | *required* |

**Returns:** `None`

### `build_plugin`

```python
def build_plugin(output_dir: str, engagement_level: str) -> None:
```

Generate a Claude Code plugin directory from TappsMCP templates.

Creates a complete plugin with skills, agents, hooks, MCP config,
and platform rules that can be submitted to the Claude Code marketplace.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `output_dir` | `str` |  | *required* |
| `engagement_level` | `str` |  | *required* |

**Returns:** `None`

### `build_cursor_plugin`

```python
def build_cursor_plugin(output_dir: str, plugin_version: str | None) -> None:
```

Generate the Cursor marketplace plugin bundle from TappsMCP templates.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `output_dir` | `str` |  | *required* |
| `plugin_version` | `str \| None` |  | *required* |

**Returns:** `None`

### `validate_skills_cmd`

```python
def validate_skills_cmd(skills_path: str, platform: str) -> None:
```

Validate SKILL.md frontmatter against Agent Skills spec (Epic 76.4).

Checks name (1-64 chars, lowercase+hyphens), description (1-1024 chars),
and allowed-tools format (space-delimited for Claude). Run from project root
or pass --path to a skills directory.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `skills_path` | `str` |  | *required* |
| `platform` | `str` |  | *required* |

**Returns:** `None`

### `cleanup_hook_backups`

```python
def cleanup_hook_backups(project_root: str, dry_run: bool) -> None:
```

Remove legacy hook ``*.pre-upgrade.*`` sidecars from ``.claude/hooks`` and ``.cursor/hooks``.

Also prunes excess copies under ``.tapps-mcp/hook-backups/`` (keeps two per hook).
Runs automatically at the end of ``tapps-mcp upgrade``; use this for one-off cleanup.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |
| `dry_run` | `bool` |  | *required* |

**Returns:** `None`

### `bump_stamps`

```python
def bump_stamps(project_root: str, dry_run: bool) -> None:
```

Bump AGENTS.md / CLAUDE.md version stamps to the installed package version.

Use when those files are in ``upgrade_skip_files`` and doctor reports a
stamp mismatch. Does not merge template content — stamps only.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |
| `dry_run` | `bool` |  | *required* |

**Returns:** `None`

### `rollback`

```python
def rollback(project_root: str, backup_id: str | None, list_backups: bool, dry_run: bool) -> None:
```

Restore configuration files from a pre-upgrade backup.

By default restores from the latest backup.
Use --backup-id to select a specific one, or --list to see all.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |
| `backup_id` | `str \| None` |  | *required* |
| `list_backups` | `bool` |  | *required* |
| `dry_run` | `bool` |  | *required* |

**Returns:** `None`

### `show_config`

```python
def show_config(project_root: str) -> None:
```

Dump the current effective TappsMCP configuration as YAML.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |

**Returns:** `None`

### `memory`

```python
def memory() -> None:
```

Manage shared project memories (no MCP server required).

**Returns:** `None`

### `memory_list`

```python
def memory_list(tier: str | None, scope: str | None, as_json: bool) -> None:
```

List all memory entries with optional filters.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tier` | `str \| None` |  | *required* |
| `scope` | `str \| None` |  | *required* |
| `as_json` | `bool` |  | *required* |

**Returns:** `None`

### `memory_save`

```python
def memory_save(key: str, value: str, tier: str, tags: str, memory_group: str | None) -> None:
```

Save a memory entry via BrainBridge (HTTP or in-process DSN).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `key` | `str` |  | *required* |
| `value` | `str` |  | *required* |
| `tier` | `str` |  | *required* |
| `tags` | `str` |  | *required* |
| `memory_group` | `str \| None` |  | *required* |

**Returns:** `None`

### `memory_get`

```python
def memory_get(key: str) -> None:
```

Retrieve a memory entry by key via BrainBridge (HTTP or in-process DSN).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `key` | `str` |  | *required* |

**Returns:** `None`

### `memory_recall`

```python
def memory_recall(query: str, project_root: str, max_results: int, min_score: float, recall_keys: tuple[str, ...]) -> None:
```

Search memories via BrainBridge and output XML for auto-recall injection.

Used by the memory_auto_recall hook (Epic 65.4 / TAP-414). Outputs
``<memory_context>...</memory_context>`` to stdout. When no
``TAPPS_BRAIN_DATABASE_URL`` is configured, exits 0 silently (degraded
mode — auto-recall just injects nothing).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `query` | `str` |  | *required* |
| `project_root` | `str` |  | *required* |
| `max_results` | `int` |  | *required* |
| `min_score` | `float` |  | *required* |
| `recall_keys` | `tuple[str, ...]` |  | *required* |

**Returns:** `None`

### `memory_search`

```python
def memory_search(query: str, limit: int, as_json: bool) -> None:
```

Search memories via BrainBridge (HTTP or DSN) or local MemoryStore fallback.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `query` | `str` |  | *required* |
| `limit` | `int` |  | *required* |
| `as_json` | `bool` |  | *required* |

**Returns:** `None`

### `memory_delete`

```python
def memory_delete(key: str) -> None:
```

Delete a memory entry.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `key` | `str` |  | *required* |

**Returns:** `None`

### `memory_import`

```python
def memory_import(file_path: str, overwrite: bool) -> None:
```

Import memories from a JSON file.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `file_path` | `str` |  | *required* |
| `overwrite` | `bool` |  | *required* |

**Returns:** `None`

### `memory_export`

```python
def memory_export(file_path: str, export_format: str, tier: str | None, scope: str | None, min_confidence: float) -> None:
```

Export memories to a JSON or Markdown file.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `file_path` | `str` |  | *required* |
| `export_format` | `str` |  | *required* |
| `tier` | `str \| None` |  | *required* |
| `scope` | `str \| None` |  | *required* |
| `min_confidence` | `float` |  | *required* |

**Returns:** `None`

### `memory_reseed`

```python
def memory_reseed(confirm: bool) -> None:
```

Re-seed memories from the project profile (auto-seeded entries only).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `confirm` | `bool` |  | *required* |

**Returns:** `None`

### `lookup_docs_cmd`

```python
def lookup_docs_cmd(library: str, topic: str, mode: str, raw: bool) -> None:
```

Look up library documentation (no MCP server required).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `library` | `str` |  | *required* |
| `topic` | `str` |  | *required* |
| `mode` | `str` |  | *required* |
| `raw` | `bool` |  | *required* |

**Returns:** `None`

### `replace_exe_cmd`

```python
def replace_exe_cmd(new_exe_path: str) -> None:
```

Replace the running exe with a new version (frozen exe only).

Renames the currently running tapps-mcp.exe to .old, then copies
NEW_EXE_PATH to the original location. Old processes keep running
from the renamed file. New sessions pick up the new binary.

The .old backup is cleaned up automatically on next startup.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `new_exe_path` | `str` |  | *required* |

**Returns:** `None`

### `migrate_memory_cmd`

```python
def migrate_memory_cmd(project_root: str, dry_run: bool, validate_only: bool, rollback_run_id: str | None) -> None:
```

Migrate .tapps-mcp/memory/*.db entries into tapps-brain (TAP-415).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `project_root` | `str` |  | *required* |
| `dry_run` | `bool` |  | *required* |
| `validate_only` | `bool` |  | *required* |
| `rollback_run_id` | `str \| None` |  | *required* |

**Returns:** `None`

### `release_update_cmd`

```python
def release_update_cmd(version: str, prev_version: str, bump_type: str, team: str, project: str, dry_run: bool) -> None:
```

Generate and validate a Linear release update document body (TAP-1112).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `version` | `str` |  | *required* |
| `prev_version` | `str` |  | *required* |
| `bump_type` | `str` |  | *required* |
| `team` | `str` |  | *required* |
| `project` | `str` |  | *required* |
| `dry_run` | `bool` |  | *required* |

**Returns:** `None`

---

*Documentation coverage: 100.0%*
*Quality: 46 functions missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common`

Common utilities shared across TappsMCP modules.

Re-exports from tapps_core.common for backward compatibility.

> **Note:** This module re-exports symbols from other modules. See source modules for full documentation.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.constants`

Shared constants - re-exported from tapps_core.common.constants.

This module re-exports all public symbols for backward compatibility.
The canonical implementation lives in ``tapps_core.common.constants``.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.developer_workflow`

Canonical developer workflow content: Setup, Update, Daily, and when-to-use.

Single source of truth for tapps_session_start quick_start, tapps_init
developer_workflow response, and docs/TAPPS_WORKFLOW.md.

## Functions

### `get_developer_workflow_dict`

```python
def get_developer_workflow_dict(*, setup_done: bool = True) -> dict[str, object]:
```

Return the developer_workflow object for tapps_init (and similar) responses.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `setup_done` | `bool` |  | True |

**Returns:** `dict[str, object]`

### `render_workflow_md`

```python
def render_workflow_md() -> str:
```

Render docs/TAPPS_WORKFLOW.md content.

**Returns:** `str`

## Constants

| Name | Type | Value |
|------|------|-------|
| `DAILY_STEPS` | `` | `['1. FIRST: Call tapps_session_start() to initialize the session', "2. BEFORE using any library API: Call tapps_lookup_docs(library='<name>')", "3. DURING edits: Call tapps_quick_check(file_path='<path>') after each change", '4. BEFORE declaring done: Call tapps_validate_changed() - all gates MUST pass', "5. FINAL step: Call tapps_checklist(task_type='<type>') to verify completeness"]` |
| `RECOMMENDED_WORKFLOW_TEXT` | `` | `'FIRST: Call tapps_session_start() to initialize. BEFORE using any library: Call tapps_lookup_docs(). AFTER editing Python files: Call tapps_score_file(quick=True) or tapps_quick_check(). BEFORE declaring done: Call tapps_validate_changed() or tapps_quality_gate(). FINAL step: Call tapps_checklist().'` |
| `SETUP_STEPS` | `` | `['Run tapps_doctor (optional) to verify MCP and checkers.', 'Run tapps_init to bootstrap AGENTS.md, rules, hooks, skills.']` |
| `UPDATE_STEP` | `` | `'After upgrading TappsMCP (or pulling a new version), run tapps_upgrade. Optionally run tapps_doctor to verify.'` |
| `WHEN_TO_USE` | `` | `[('tapps_lookup_docs', 'Before using any external library API.'), ('tapps_impact_analysis', "Before changing or removing a file's public API."), ('tapps_security_scan', 'For auth, API routes, secrets, input validation.'), ('tapps_validate_config', 'When editing Dockerfile, docker-compose, or infra config.')]` |

---

*Documentation coverage: 100.0%*
*Quality: 2 functions missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.elicitation`

MCP elicitation helpers for interactive user input.

MCP elicitation is a protocol extension that allows servers to request
structured input from the user via the host's native UI during tool
execution.  Currently only Cursor implements this extension.

On unsupported clients the helpers degrade gracefully — they return
``None`` instead of raising, so callers can fall back to defaults.

## Classes

### `PresetElicitation`(BaseModel)

Schema for quality gate preset selection.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `preset` | `str` | `Field(description='Quality gate preset', json_schema_extra={'enum': ['development', 'staging', 'production'], 'enumNames': ['Development (lenient — score >= 60)', 'Staging (moderate — score >= 75)', 'Production (strict — score >= 90)']})` |

### `InitConfirmation`(BaseModel)

Schema for tapps_init confirmation.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `confirm` | `bool` | `Field(description='Confirm TappsMCP initialization')` |

### `WizardQualityPreset`(BaseModel)

Schema for wizard quality preset selection.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `preset` | `str` | `Field(description='Quality standard', json_schema_extra={'enum': ['standard', 'strict', 'framework'], 'enumNames': ['Standard (70+ score, recommended for most projects)', 'Strict (80+ score, for production codebases)', 'Framework (75+ score, for library/framework development)']})` |

### `WizardEngagementLevel`(BaseModel)

Schema for wizard engagement level selection.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `level` | `str` | `Field(description='Engagement level', json_schema_extra={'enum': ['high', 'medium', 'low'], 'enumNames': ['High (mandatory enforcement — blocks without validation)', 'Medium (balanced — reminders and nudges)', 'Low (optional guidance — minimal intervention)']})` |

### `WizardAgentTeams`(BaseModel)

Schema for wizard agent teams selection.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `enabled` | `bool` | `Field(description='Enable agent team hooks')` |

### `WizardSkillTier`(BaseModel)

Schema for wizard skill tier selection.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `tier` | `str` | `Field(description='Skill tier', json_schema_extra={'enum': ['core', 'full'], 'enumNames': ['Core only (score, gate, validate, security — 4 skills)', 'Full (all 7 skills including research, memory, review pipeline)']})` |

### `WizardPromptHooks`(BaseModel)

Schema for wizard prompt hooks selection.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `enabled` | `bool` | `Field(description='Enable AI-powered quality judgment')` |

### `WizardOtherMcps`(BaseModel)

Schema for wizard 'add other MCPs' prompt.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `enabled` | `bool` | `Field(description='Show guidance on adding complementary MCPs (GitHub, YouTube, etc.)')` |

### `WizardConfigScope`(BaseModel)

Schema for wizard config scope selection (Epic 47).

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `scope` | `str` | `Field(description='Config scope', json_schema_extra={'enum': ['project', 'user'], 'enumNames': ['Project scope — .mcp.json in project root (recommended)', 'User scope — ~/.claude.json (global, affects all projects)']})` |

### `WizardResult`

Aggregated answers from the interactive first-run wizard.

**Methods:**

#### `__init__`

```python
def __init__(self) -> None:
```

**Returns:** `None`

## Functions

### async `elicit_preset`

```python
async def elicit_preset(ctx: Context) -> str | None:
```

Ask the user to select a quality gate preset via elicitation.

Returns the selected preset string (e.g. ``"staging"``) if the user
accepted, or ``None`` if declined, cancelled, or unsupported.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `ctx` | `Context` |  | *required* |

**Returns:** `str | None`

### async `elicit_init_confirmation`

```python
async def elicit_init_confirmation(ctx: Context, project_root: str) -> bool | None:
```

Ask the user to confirm tapps_init before writing files.

Returns ``True`` if confirmed, ``False`` if explicitly declined,
or ``None`` if unsupported/cancelled/timed out.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `ctx` | `Context` |  | *required* |
| `project_root` | `str` |  | *required* |

**Returns:** `bool | None`

### async `run_init_wizard`

```python
async def run_init_wizard(ctx: Context[Any, Any, Any], *, claude_code_detected: bool = True) -> WizardResult:
```

Run the interactive wizard via MCP elicitation.

Returns a :class:[`WizardResult`](#wizardresult) with the user's selections.
If elicitation is unsupported or the user cancels, returns defaults
with ``completed=False``.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `ctx` | `Context[Any, Any, Any]` | MCP context for elicitation. | *required* |
| `claude_code_detected` | `bool` |  | True |

**Returns:** `WizardResult`

---

*Documentation coverage: 92.9%*
*Quality: 3 functions missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.exceptions`

Exception hierarchy - re-exported from tapps_core.common.exceptions.

This module re-exports all public symbols for backward compatibility.
The canonical implementation lives in ``tapps_core.common.exceptions``.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.logging`

Structured logging - re-exported from tapps_core.common.logging.

This module re-exports all public symbols for backward compatibility.
The canonical implementation lives in ``tapps_core.common.logging``.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.models`

Shared Pydantic v2 models - re-exported from tapps_core.common.models.

This module re-exports all public symbols for backward compatibility.
The canonical implementation lives in ``tapps_core.common.models``.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.nudges`

Next-step nudge engine - computes actionable suggestions based on session state.

Every tool response includes ``next_steps`` telling the LLM exactly what
to call next.  The nudge engine reads ``CallTracker`` to know which tools
have already been called and produces a short, imperative list.

When ``tapps_mcp.tools.checklist`` is unavailable (e.g. incomplete binary),
CallTracker is not loaded and nudges/pipeline progress are omitted.

## Functions

### `compute_next_steps`

```python
def compute_next_steps(tool_name: str, context: dict[str, Any] | None = None) -> list[str]:
```

Return the single highest-impact next-step string for the given tool.

STORY-101.5: Collects all matching nudges, ranks by impact score, and
returns only the top-1 so agents always get one clear action — not a
noisy list to triage.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tool_name` | `str` | The tool that was just called. | *required* |
| `context` | `dict[str, Any] \| None` | Optional dict with tool-specific state (e.g. ``{"security_issue_count": 2, "gate_passed": False}``). | None |

**Returns:** `list[str]` - List with at most 1 short imperative string.

### `compute_suggested_workflow`

```python
def compute_suggested_workflow(tool_name: str, context: dict[str, Any] | None = None) -> list[str] | None:
```

Return an ordered workflow suggestion for the given tool, or None.

Unlike :func:[`compute_next_steps`](#compute-next-steps) which returns single-step nudges,
this returns a multi-step workflow when the tool output indicates a
non-trivial follow-up sequence (e.g. review pipeline for many changed
files, or fix-rescore-gate loop for low scores).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tool_name` | `str` | The tool that was just called. | *required* |
| `context` | `dict[str, Any] \| None` | Optional dict with tool-specific state. | None |

**Returns:** `list[str] | None` - Ordered list of workflow step strings, or ``None`` if no workflow
    is triggered.

### `compute_pipeline_progress`

```python
def compute_pipeline_progress() -> dict[str, Any]:
```

Return pipeline stage completion status based on which tools have been called.

**Returns:** `dict[str, Any]` - Dict with ``completed_stages``, ``next_stage``, ``tools_called``,
    and ``total_calls``.

## Constants

| Name | Type | Value |
|------|------|-------|
| `NudgeRule` | `` | `tuple[Any, str, int]` |

---

*Documentation coverage: 100.0%*
*Quality: 3 functions missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.output_schemas`

Structured output schemas for MCP tool responses.

Each model defines the machine-parseable JSON that tools return alongside
human-readable text via the MCP ``structuredContent`` mechanism.

## Classes

### `StructuredOutput`(BaseModel)

Base for all structured outputs.

**Methods:**

#### classmethod `to_output_schema`

```python
def to_output_schema(cls) -> dict[str, Any]:
```

Return JSON schema dict for MCP tool registration.

**Returns:** `dict[str, Any]`

#### `to_structured_content`

```python
def to_structured_content(self) -> dict[str, Any]:
```

Serialize for MCP structuredContent response.

**Returns:** `dict[str, Any]`

### `CategoryScoreOutput`(BaseModel)

Single category score in structured output.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `name` | `str` |  |
| `score` | `float` | `Field(ge=0.0, le=10.0)` |
| `weight` | `float` | `Field(ge=0.0, le=1.0)` |
| `suggestions` | `list[str]` | `Field(default_factory=list)` |

### `ScoreFileOutput`(StructuredOutput)

Structured output for tapps_score_file.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `file_path` | `str` |  |
| `overall_score` | `float` | `Field(ge=0.0, le=100.0)` |
| `categories` | `dict[str, CategoryScoreOutput]` | `Field(default_factory=dict)` |
| `lint_issue_count` | `int` | `0` |
| `type_issue_count` | `int` | `0` |
| `security_issue_count` | `int` | `0` |
| `degraded` | `bool` | `False` |
| `tool_errors` | `dict[str, str]` | `Field(default_factory=dict)` |
| `suggestions` | `list[str]` | `Field(default_factory=list)` |

### `GateFailure`(BaseModel)

A single quality gate failure.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `category` | `str` |  |
| `actual` | `float` |  |
| `threshold` | `float` |  |
| `message` | `str` | `''` |

### `QualityGateOutput`(StructuredOutput)

Structured output for tapps_quality_gate.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `file_path` | `str` |  |
| `passed` | `bool` |  |
| `preset` | `str` |  |
| `overall_score` | `float` | `Field(ge=0.0, le=100.0)` |
| `threshold` | `float` | `Field(ge=0.0, le=100.0)` |
| `scores` | `dict[str, float]` | `Field(default_factory=dict)` |
| `failures` | `list[GateFailure]` | `Field(default_factory=list)` |
| `warnings` | `list[str]` | `Field(default_factory=list)` |
| `suggestions` | `list[str]` | `Field(default_factory=list)` |

### `QuickCheckOutput`(StructuredOutput)

Structured output for tapps_quick_check.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `file_path` | `str` |  |
| `overall_score` | `float` | `Field(ge=0.0, le=100.0)` |
| `gate_passed` | `bool` |  |
| `gate_preset` | `str` |  |
| `security_passed` | `bool` |  |
| `lint_issue_count` | `int` | `0` |
| `security_issue_count` | `int` | `0` |
| `suggestions` | `list[str]` | `Field(default_factory=list)` |
| `complexity_hint` | `dict[str, Any] | None` | `None` |
| `gate_failures` | `list[dict[str, Any]]` | `Field(default_factory=list)` |
| `quick_categories` | `dict[str, float]` | `Field(default_factory=dict)` |
| `fixes_applied` | `int | None` | `None` |
| `recurring_quality_memory_events` | `list[dict[str, str]]` | `Field(default_factory=list)` |

### `SecurityFindingOutput`(BaseModel)

A single security finding in structured output.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `code` | `str` |  |
| `message` | `str` |  |
| `file` | `str` |  |
| `line` | `int` |  |
| `severity` | `str` | `'medium'` |
| `confidence` | `str` | `'medium'` |

### `SecurityScanOutput`(StructuredOutput)

Structured output for tapps_security_scan.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `file_path` | `str` |  |
| `passed` | `bool` |  |
| `total_issues` | `int` | `0` |
| `critical_count` | `int` | `0` |
| `high_count` | `int` | `0` |
| `medium_count` | `int` | `0` |
| `low_count` | `int` | `0` |
| `bandit_available` | `bool` | `True` |
| `findings` | `list[SecurityFindingOutput]` | `Field(default_factory=list)` |

### `FileValidationResult`(BaseModel)

Per-file result in validate_changed.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `file_path` | `str` |  |
| `score` | `float` | `0.0` |
| `gate_passed` | `bool` | `False` |
| `security_passed` | `bool` | `True` |

### `ValidateChangedOutput`(StructuredOutput)

Structured output for tapps_validate_changed.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `files` | `list[FileValidationResult]` | `Field(default_factory=list)` |
| `overall_passed` | `bool` | `False` |
| `total_files` | `int` | `0` |
| `passed_count` | `int` | `0` |
| `failed_count` | `int` | `0` |
| `security_depth` | `str` | `'basic'` |
| `impact_summary` | `dict[str, Any] | None` | `None` |

### `ConfigFindingOutput`(BaseModel)

A single config validation finding in structured output.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `severity` | `str` |  |
| `message` | `str` |  |
| `line` | `int | None` | `None` |
| `category` | `str` | `'general'` |

### `ValidateConfigOutput`(StructuredOutput)

Structured output for tapps_validate_config.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `file_path` | `str` |  |
| `config_type` | `str` |  |
| `valid` | `bool` |  |
| `finding_count` | `int` | `0` |
| `critical_count` | `int` | `0` |
| `warning_count` | `int` | `0` |
| `findings` | `list[ConfigFindingOutput]` | `Field(default_factory=list)` |
| `suggestions` | `list[str]` | `Field(default_factory=list)` |

### `ImpactOutput`(StructuredOutput)

Structured output for tapps_impact_analysis.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `changed_file` | `str` |  |
| `change_type` | `str` |  |
| `severity` | `str` |  |
| `total_affected` | `int` | `0` |
| `direct_dependents` | `list[str]` | `Field(default_factory=list)` |
| `test_files` | `list[str]` | `Field(default_factory=list)` |
| `recommendations` | `list[str]` | `Field(default_factory=list)` |
| `memory_context` | `list[dict[str, Any]]` | `Field(default_factory=list, description='Memory search hits for the target file (Epic M4.4).')` |

### `ChecklistOutput`(StructuredOutput)

Structured output for tapps_checklist.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `task_type` | `str` |  |
| `complete` | `bool` |  |
| `called` | `list[str]` | `Field(default_factory=list)` |
| `missing_required` | `list[str]` | `Field(default_factory=list)` |
| `missing_recommended` | `list[str]` | `Field(default_factory=list)` |
| `total_calls` | `int` | `0` |
| `checklist_policy_version` | `str | None` | `None` |
| `resolved_policy_task_type` | `str | None` | `None` |
| `checklist_session_id` | `str | None` | `None` |
| `auto_run_results` | `dict[str, Any] | None` | `None` |

### `FeedbackOutput`(StructuredOutput)

Structured output for tapps_feedback.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `recorded` | `bool` |  |
| `tool_name` | `str` |  |
| `helpful` | `bool` |  |
| `duplicate_skipped` | `bool` | `False` |
| `weight_adjusted` | `bool` | `False` |

### `StatsOutput`(StructuredOutput)

Structured output for tapps_stats.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `period` | `str` |  |
| `total_calls` | `int` | `0` |
| `success_rate` | `float` | `0.0` |
| `recommendations` | `list[str]` | `Field(default_factory=list)` |

### `DashboardOutput`(StructuredOutput)

Structured output for tapps_dashboard.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `time_range_applied` | `str` | `'7d'` |
| `total_tool_calls` | `int` | `0` |
| `gate_pass_rate` | `float` | `0.0` |
| `active_alerts` | `int` | `0` |

### `DeadCodeOutput`(StructuredOutput)

Structured output for tapps_dead_code.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `file_path` | `str` | `''` |
| `scope` | `str` | `'file'` |
| `total_findings` | `int` | `0` |
| `files_scanned` | `int` | `1` |
| `degraded` | `bool` | `False` |
| `min_confidence` | `int` | `80` |
| `by_type` | `dict[str, list[dict[str, Any]]]` | `Field(default_factory=dict)` |
| `type_counts` | `dict[str, int]` | `Field(default_factory=dict)` |
| `summary` | `str` | `''` |

### `ProfileOutput`(StructuredOutput)

Structured output for tapps_project_profile.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `project_root` | `str` |  |
| `project_type` | `str` |  |
| `project_type_confidence` | `float` | `Field(ge=0.0, le=1.0)` |
| `has_ci` | `bool` | `False` |
| `has_docker` | `bool` | `False` |
| `has_tests` | `bool` | `False` |
| `test_frameworks` | `list[str]` | `Field(default_factory=list)` |
| `package_managers` | `list[str]` | `Field(default_factory=list)` |
| `quality_recommendations` | `list[str]` | `Field(default_factory=list)` |

### `SessionStartOutput`(StructuredOutput)

Structured output for tapps_session_start.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `server_version` | `str` |  |
| `project_root` | `str` |  |
| `project_type` | `str | None` | `None` |
| `quality_preset` | `str` | `'standard'` |
| `installed_checkers` | `list[str]` | `Field(default_factory=list)` |
| `checker_environment` | `str` | `'mcp_server'` |
| `has_ci` | `bool` | `False` |
| `has_docker` | `bool` | `False` |
| `has_tests` | `bool` | `False` |

### `ToolError`(BaseModel)

Standard error block emitted by ``server_helpers.error_response``.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(extra='allow')` |
| `code` | `str` |  |
| `message` | `str` |  |
| `category` | `str | None` | `None` |
| `retryable` | `bool | None` | `None` |
| `remediation` | `str | None` | `None` |

### `SessionStartData`(BaseModel)

Fields from ``tapps_session_start.data`` that agents typically branch on.

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `model_config` |  | `ConfigDict(extra='allow')` |
| `project_root` | `str | None` | `None` |
| `server` | `dict[str, Any] | None` | `None` |
| `configuration` | `dict[str, Any] | None` | `None` |
| `installed_checkers` | `list[dict[str, Any]]` | `Field(default_factory=list)` |
| `checker_environment` | `str | None` | `None` |
| `cached` | `bool` | `False` |

### `TappsSessionStartResponse`(_ToolEnvelope)

Output schema for ``tapps_session_start`` (B1).

**Class Variables:**

| Name | Type | Default |
|------|------|---------|
| `tool` | `str` | `'tapps_session_start'` |
| `data` | `SessionStartData | None` | `None` |

## Functions

### `get_output_schema`

```python
def get_output_schema(tool_name: str) -> dict[str, Any] | None:
```

Get the JSON output schema for a tool, or None if not registered.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `tool_name` | `str` |  | *required* |

**Returns:** `dict[str, Any] | None`

## Constants

| Name | Type | Value |
|------|------|-------|
| `OUTPUT_SCHEMA_REGISTRY` | `dict[str, type[StructuredOutput]]` | `{}` |

---

*Documentation coverage: 100.0%*
*Quality: 1 function missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.pipeline_models`

Pipeline stage definitions - re-exported from tapps_core.common.pipeline_models.

This module re-exports all public symbols for backward compatibility.
The canonical implementation lives in ``tapps_core.common.pipeline_models``.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.common.utils`

Shared utility functions - re-exported from tapps_core.common.utils.

This module re-exports all public symbols for backward compatibility.
The canonical implementation lives in ``tapps_core.common.utils``.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.config`

Configuration - re-exported from tapps_core.config for backward compatibility.

> **Note:** This module re-exports symbols from other modules. See source modules for full documentation.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.config.settings`

TappsMCP configuration system - re-exported from tapps_core.config.settings.

This module re-exports all public symbols for backward compatibility.
The canonical implementation lives in ``tapps_core.config.settings``.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.diagnostics`

Startup diagnostics - local-only health checks for TappsMCP subsystems.

## Functions

### `check_context7`

```python
def check_context7(api_key: SecretStr | None) -> Context7Diagnostic:
```

Check whether a Context7 API key is configured.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `api_key` | `SecretStr \| None` |  | *required* |

**Returns:** `Context7Diagnostic`

### `check_cache`

```python
def check_cache(cache_dir: Path) -> CacheDiagnostic:
```

Check cache directory existence, writability, and stats.

Only instantiates ``KBCache`` if the directory already exists to avoid
auto-creating it as a side effect.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `cache_dir` | `Path` |  | *required* |

**Returns:** `CacheDiagnostic`

### `check_knowledge_base`

```python
def check_knowledge_base() -> KnowledgeBaseDiagnostic:
```

Expert system removed (EPIC-94). Returns empty diagnostic.

**Returns:** `KnowledgeBaseDiagnostic`

### `install_drift_remediation_hint`

```python
def install_drift_remediation_hint(entries: list[InstallDriftEntry], *, uses_blue_green: bool) -> str:
```

Build context-aware remediation copy for install drift (TAP-2129).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `entries` | `list[InstallDriftEntry]` |  | *required* |
| `uses_blue_green` | `bool` |  | *required* |

**Returns:** `str`

### `format_upgrade_blocked_by_drift`

```python
def format_upgrade_blocked_by_drift(drift: InstallDriftDiagnostic) -> str:
```

Human-readable upgrade gate message when install drift is detected (TAP-2200).

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `drift` | `InstallDriftDiagnostic` |  | *required* |

**Returns:** `str`

### `check_install_drift`

```python
def check_install_drift() -> InstallDriftDiagnostic:
```

TAP-2129: detect drift between in-process package versions and deployed CLIs.

When the dev-monorepo blue/green ``~/.tapps-mcp/current`` layout is active,
probes ``current/bin/*`` instead of the legacy ``uv tool install`` shims.

**Returns:** `InstallDriftDiagnostic`

### `collect_diagnostics`

```python
def collect_diagnostics(api_key: SecretStr | None, cache_dir: Path) -> StartupDiagnostics:
```

Run all diagnostic checks and return a single ``StartupDiagnostics``.

**Parameters:**

| Name | Type | Description | Default |
|------|------|-------------|---------|
| `api_key` | `SecretStr \| None` |  | *required* |
| `cache_dir` | `Path` |  | *required* |

**Returns:** `StartupDiagnostics`

---

*Documentation coverage: 100.0%*
*Quality: 7 functions missing examples*

*Auto-generated by DocsMCP on 2026-06-22*


---

# `packages.tapps-mcp.src.tapps_mcp.distribution`

Distribution and setup utilities for TappsMCP.

> **Note:** This module re-exports symbols from other modules. See source modules for full documentation.

---

*Documentation coverage: 0.0%*

*Auto-generated by DocsMCP on 2026-06-22*
