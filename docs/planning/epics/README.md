# TappsMCP Epics

Epics live in **Linear**, not in this repository. Per `.claude/rules/linear-standards.md`, create and update epics through the `linear-issue` skill (`docs_generate_epic` → `docs_validate_linear_issue` → `save_issue`).

**Backlog:** [TappsMCP Platform on Linear](https://linear.app/tappscodingagents/project/tappsmcp-platform)

The code and ADRs in `docs/adr/` are the source of truth for shipped behavior.
