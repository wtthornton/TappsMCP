# Architectural Archetype — Pattern Card

Auto-classified by `docs-mcp`'s deterministic pattern classifier. tapps-mcp is detected as **microservice** with confidence 0.70 (3 `pyproject.toml` files in `packages/tapps-core`, `packages/tapps-mcp`, `packages/docs-mcp`).

Auto-generated by `docs_generate_diagram(diagram_type="pattern_card", scope="project", format="mermaid")`.

```mermaid
flowchart TD
    header["MICROSERVICE (confidence 0.70)<br/>3 pyproject.toml files found: packages/tapps-core, packages/tapps-mcp, packages/docs-mcp"]
    p0_basic_quality_check["basic_quality_check"]:::infra
    p1_build_epic_stories["build_epic_stories"]:::presentation
    p2_bump_versions["bump-versions"]:::infra
    p3_combined_server["combined_server"]:::presentation
    p4_compare["compare"]:::infra
    p5_docs_mcp["docs_mcp"]:::infra
    p6_generate_tools_json["generate-tools-json"]:::business
    p7_integration["integration"]:::infra
    header --> p1_build_epic_stories
    p1_build_epic_stories --> p6_generate_tools_json
    p6_generate_tools_json --> p0_basic_quality_check
    subgraph legend["Legend"]
        L_presentation["Presentation"]:::presentation
        L_business["Business"]:::business
        L_data["Data"]:::data
        L_infra["Infra"]:::infra
    end
    classDef presentation fill:#F5A9D0,stroke:#333,color:#000
    classDef business fill:#14B8A6,stroke:#333,color:#fff
    classDef data fill:#9333EA,stroke:#333,color:#fff
    classDef infra fill:#6B7280,stroke:#333,color:#fff
```

**Note on "microservice":** the classifier reports microservice because multiple `pyproject.toml` files mark independently versioned packages. The runtime topology is *not* multi-process: tapps-mcp and docs-mcp are independent MCP servers, and tapps-core is a shared library imported by both. The shared dependency is `tapps-brain`, which *is* a separate service (Postgres @ :8080).
