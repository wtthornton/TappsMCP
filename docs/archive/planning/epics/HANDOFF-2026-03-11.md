# Handoff — 2026-03-11 (Post Epic 74 & 79)

Session closed Epic 74, completed Epic 79 (79.4–79.6), reconciled Epic 65, and marked Epic 66 complete. Use this handoff to continue with a new agent.

---

## What Was Done This Session

| Area | Change |
|------|--------|
| **Epic 74** | Marked **Complete** (2026-03-11). All 5 stories were already implemented; epic doc and README updated. |
| **Epic 79** | Completed remaining stories: **79.4** (TOOL-SUBSETS-AND-DOCKER-FILTERING.md), **79.5** (role presets reviewer/planner/frontend/developer in config + server), **79.6** (Docker profiles tapps-reviewer, tapps-planning, tapps-frontend, tapps-developer). Epic and README set to Complete. |
| **Epic 65** | README reconciled: 65.1, 65.2, 65.3, 65.5, 65.6, 65.9, 65.10, 65.12, 65.14, 65.15, 65.17 marked **Complete**; 65.4, 65.7, 65.8, 65.11, 65.13, 65.16 remain **Proposed**. |
| **Epic 66** | Marked **Complete** (66.1 path_hint/next_steps and 66.2 checklist note already in code). |

### Key Files Touched

- `docs/planning/epics/EPIC-74-CONSUMER-FEEDBACK-AUTOMATION-PIPELINE-UX.md` — Status Complete
- `docs/planning/epics/EPIC-79-MCP-TOOL-COUNT-CURATION.md` — Status Complete
- `docs/planning/epics/README.md` — Epic 74, 66, 79 Complete; Epic 65 table updated
- `docs/planning/TOOL-SUBSETS-AND-DOCKER-FILTERING.md` — **New** (79.4)
- `packages/tapps-core/src/tapps_core/config/settings.py` — `tool_preset` extended with reviewer, planner, frontend, developer
- `packages/tapps-mcp/src/tapps_mcp/server.py` — TOOL_PRESET_REVIEWER/PLANNER/FRONTEND/DEVELOPER + _resolve_allowed_tools
- `packages/tapps-mcp/tests/unit/test_enabled_tools_config.py` — 4 new preset tests
- `docker-mcp/profiles/tapps-reviewer.yaml`, `tapps-planning.yaml`, `tapps-frontend.yaml`, `tapps-developer.yaml` — **New**
- `docker-mcp/README.md`, `AGENTS.md`, `docs/planning/epics/OPEN-EPICS-AND-STORIES-REVIEW.md` — Updated

---

## Open Epics (Recommended Next)

| Epic | Priority | LOE | Description |
|------|----------|-----|-------------|
| **75** | P2 | ~3–4 wk | LLM Artifact Structure & Prompt Generation — PromptConfig, docs_generate_prompt, common schema, compact LLM view |
| **76** | P2 | ~1.5–2 wk | Skills Spec Compliance — description ≤1024, allowed-tools format, optional validator |
| **77** | P3 | ~2–4 days | Agency-Agents Integration — document coexistence + optional init hint |
| **78** | P2 | ~1–2 wk | Canonical Persona Injection — tapps_get_canonical_persona, rule prepend, prompt-injection docs |
| **65** (remaining) | P1–P2 | varies | 65.4, 65.7, 65.8, 65.11, 65.13, 65.16 still Proposed (auto-recall, vector provider, RRF, procedural tier, relationship retrieval, context budget) |
| **70–73** | P2–P3 | Draft | Expert personas, critical rules, knowledge enrichment, communication style |

Full list and story links: `docs/planning/epics/README.md`. Review of open vs complete: `docs/planning/epics/OPEN-EPICS-AND-STORIES-REVIEW.md`.

---

## Handoff Prompt for Next Session

Copy the block below into a new session to continue.

---

### TappsMCP Project Handoff — 2026-03-11

**TappsMCP** is an MCP server providing deterministic code quality tools to LLMs. It's a **uv workspace monorepo** with three packages:

- **tapps-core** (`packages/tapps-core/`) — Shared infrastructure (config, security, logging, memory, experts)
- **tapps-mcp** (`packages/tapps-mcp/`) — Code quality MCP server (29 tools; supports enabled_tools, tool_preset including role presets)
- **docs-mcp** (`packages/docs-mcp/`) — Documentation MCP server (22 tools; supports enabled_tools / tool_preset)

### Recently Completed (2026-03-11)

- **Epic 74** — Consumer Feedback & Pipeline UX (batch quick_check, checklist compact/JSON, validate_changed base_ref warning + traceability, MCP config validation). Closed as Complete.
- **Epic 79** — MCP Tool Count & Curation: 79.4 doc (TOOL-SUBSETS-AND-DOCKER-FILTERING.md), 79.5 role presets (reviewer, planner, frontend, developer), 79.6 Docker role profiles. Epic closed as Complete.
- **Epic 66** — Tool UX (path hints, checklist note) marked Complete.
- **Epic 65** — README reconciled; 11 of 17 sub-epics marked Complete; 6 remain Proposed.

### What to Do Next

1. **Session start:** Call `tapps_session_start` first (if TappsMCP is connected), then `tapps_memory(action="search", query="...")` to recall context.
2. **Pick an epic:** See `docs/planning/epics/README.md` and `docs/planning/epics/OPEN-EPICS-AND-STORIES-REVIEW.md`. Good next options: **Epic 75** (LLM artifacts/prompt), **Epic 76** (skills spec), **Epic 77** (agency-agents docs), or remaining **Epic 65** stories.
3. **After editing Python:** Run `tapps_quick_check(file_path)` (or batch with `file_paths`). Before declaring work complete: `tapps_validate_changed(file_paths="...")` and `tapps_checklist(task_type="feature")`.

### Development Commands

```bash
uv sync --all-packages
uv run pytest packages/tapps-core/tests/ -v
uv run pytest packages/tapps-mcp/tests/ -v
uv run pytest packages/docs-mcp/tests/ -v
uv run mypy --strict packages/tapps-core/src/tapps_core/
uv run mypy --strict packages/tapps-mcp/src/tapps_mcp/
uv run ruff check packages/*/src/
```

### Key References

- `CLAUDE.md` — Architecture, conventions, module map
- `AGENTS.md` — Tool usage; reducing tool count (tool_preset, role presets)
- `docs/planning/epics/README.md` — Epic index and status
- `docs/planning/epics/OPEN-EPICS-AND-STORIES-REVIEW.md` — Open vs complete review
- `docs/planning/TOOL-SUBSETS-AND-DOCKER-FILTERING.md` — Recommended tool subsets and Docker filtering
- `docs/planning/ROADMAP.md` — Enhancement backlog
