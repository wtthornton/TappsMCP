# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv/
.venv-pyinstaller/
venv/
env/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
htmlcov/
.coverage
coverage.xml
*.cover

# mypy
.mypy_cache/

# ruff
.ruff_cache/

# Distribution
*.tar.gz
*.whl

# OS
.DS_Store
Thumbs.db
nul

# Environment
.env
.env.local

# MCP config (local API keys, paths)
.cursor/mcp.json
.mcp.json

# TappsMCP runtime data (metrics, cache, indexes)
.tapps-mcp/
.tapps-mcp-cache/
.tapps-brain/

# UV
.python-version

# Local experiments
/tools/

# Playwright MCP test artifacts
.playwright-mcp/

# MCP registry fork (separate repo)
mcp-registry/

# Generated report screenshots
arch-report*.png
architecture-report*.png

# Agent worktrees spawned by the Agent tool (full git clones — never commit)
.claude/worktrees/

# Defensive: catch generator project-root bugs that write a workspace member
# inside itself (e.g. packages/docs-mcp/packages/docs-mcp/...).
packages/*/packages/

# tapps_upgrade timestamped backups (local rollback only; never commit)
*.pre-upgrade.*

# TAP-1712: temp / legacy session artifacts
.mcp.json.backup-*
*.log
*.tmp
.temp/
node_modules/
*.pyc
target/
.claude/settings.local.json
.claude/agent-memory/
.claude/agent-memory-local/
# Third-party skill clones (separate git repos, contains node_modules)
.claude/skills/linear/
